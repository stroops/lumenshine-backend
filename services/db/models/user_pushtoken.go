// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// UserPushtoken is an object representing the database table.
type UserPushtoken struct {
	ID         int       `boil:"id" json:"id" toml:"id" yaml:"id"`
	UserID     int       `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	DeviceType string    `boil:"device_type" json:"device_type" toml:"device_type" yaml:"device_type"`
	PushToken  string    `boil:"push_token" json:"push_token" toml:"push_token" yaml:"push_token"`
	CreatedAt  time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt  time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	UpdatedBy  string    `boil:"updated_by" json:"updated_by" toml:"updated_by" yaml:"updated_by"`

	R *userPushtokenR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L userPushtokenL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var UserPushtokenColumns = struct {
	ID         string
	UserID     string
	DeviceType string
	PushToken  string
	CreatedAt  string
	UpdatedAt  string
	UpdatedBy  string
}{
	ID:         "id",
	UserID:     "user_id",
	DeviceType: "device_type",
	PushToken:  "push_token",
	CreatedAt:  "created_at",
	UpdatedAt:  "updated_at",
	UpdatedBy:  "updated_by",
}

// UserPushtokenRels is where relationship names are stored.
var UserPushtokenRels = struct {
	User string
}{
	User: "User",
}

// userPushtokenR is where relationships are stored.
type userPushtokenR struct {
	User *UserProfile
}

// NewStruct creates a new relationship struct
func (*userPushtokenR) NewStruct() *userPushtokenR {
	return &userPushtokenR{}
}

// userPushtokenL is where Load methods for each relationship are stored.
type userPushtokenL struct{}

var (
	userPushtokenColumns               = []string{"id", "user_id", "device_type", "push_token", "created_at", "updated_at", "updated_by"}
	userPushtokenColumnsWithoutDefault = []string{"user_id", "device_type", "push_token", "updated_by"}
	userPushtokenColumnsWithDefault    = []string{"id", "created_at", "updated_at"}
	userPushtokenPrimaryKeyColumns     = []string{"id"}
)

type (
	// UserPushtokenSlice is an alias for a slice of pointers to UserPushtoken.
	// This should generally be used opposed to []UserPushtoken.
	UserPushtokenSlice []*UserPushtoken
	// UserPushtokenHook is the signature for custom UserPushtoken hook methods
	UserPushtokenHook func(boil.Executor, *UserPushtoken) error

	userPushtokenQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	userPushtokenType                 = reflect.TypeOf(&UserPushtoken{})
	userPushtokenMapping              = queries.MakeStructMapping(userPushtokenType)
	userPushtokenPrimaryKeyMapping, _ = queries.BindMapping(userPushtokenType, userPushtokenMapping, userPushtokenPrimaryKeyColumns)
	userPushtokenInsertCacheMut       sync.RWMutex
	userPushtokenInsertCache          = make(map[string]insertCache)
	userPushtokenUpdateCacheMut       sync.RWMutex
	userPushtokenUpdateCache          = make(map[string]updateCache)
	userPushtokenUpsertCacheMut       sync.RWMutex
	userPushtokenUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
)

var userPushtokenBeforeInsertHooks []UserPushtokenHook
var userPushtokenBeforeUpdateHooks []UserPushtokenHook
var userPushtokenBeforeDeleteHooks []UserPushtokenHook
var userPushtokenBeforeUpsertHooks []UserPushtokenHook

var userPushtokenAfterInsertHooks []UserPushtokenHook
var userPushtokenAfterSelectHooks []UserPushtokenHook
var userPushtokenAfterUpdateHooks []UserPushtokenHook
var userPushtokenAfterDeleteHooks []UserPushtokenHook
var userPushtokenAfterUpsertHooks []UserPushtokenHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *UserPushtoken) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range userPushtokenBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *UserPushtoken) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range userPushtokenBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *UserPushtoken) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range userPushtokenBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *UserPushtoken) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range userPushtokenBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *UserPushtoken) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range userPushtokenAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *UserPushtoken) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range userPushtokenAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *UserPushtoken) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range userPushtokenAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *UserPushtoken) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range userPushtokenAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *UserPushtoken) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range userPushtokenAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddUserPushtokenHook registers your hook function for all future operations.
func AddUserPushtokenHook(hookPoint boil.HookPoint, userPushtokenHook UserPushtokenHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		userPushtokenBeforeInsertHooks = append(userPushtokenBeforeInsertHooks, userPushtokenHook)
	case boil.BeforeUpdateHook:
		userPushtokenBeforeUpdateHooks = append(userPushtokenBeforeUpdateHooks, userPushtokenHook)
	case boil.BeforeDeleteHook:
		userPushtokenBeforeDeleteHooks = append(userPushtokenBeforeDeleteHooks, userPushtokenHook)
	case boil.BeforeUpsertHook:
		userPushtokenBeforeUpsertHooks = append(userPushtokenBeforeUpsertHooks, userPushtokenHook)
	case boil.AfterInsertHook:
		userPushtokenAfterInsertHooks = append(userPushtokenAfterInsertHooks, userPushtokenHook)
	case boil.AfterSelectHook:
		userPushtokenAfterSelectHooks = append(userPushtokenAfterSelectHooks, userPushtokenHook)
	case boil.AfterUpdateHook:
		userPushtokenAfterUpdateHooks = append(userPushtokenAfterUpdateHooks, userPushtokenHook)
	case boil.AfterDeleteHook:
		userPushtokenAfterDeleteHooks = append(userPushtokenAfterDeleteHooks, userPushtokenHook)
	case boil.AfterUpsertHook:
		userPushtokenAfterUpsertHooks = append(userPushtokenAfterUpsertHooks, userPushtokenHook)
	}
}

// OneG returns a single userPushtoken record from the query using the global executor.
func (q userPushtokenQuery) OneG() (*UserPushtoken, error) {
	return q.One(boil.GetDB())
}

// One returns a single userPushtoken record from the query.
func (q userPushtokenQuery) One(exec boil.Executor) (*UserPushtoken, error) {
	o := &UserPushtoken{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for user_pushtoken")
	}

	if err := o.doAfterSelectHooks(exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all UserPushtoken records from the query using the global executor.
func (q userPushtokenQuery) AllG() (UserPushtokenSlice, error) {
	return q.All(boil.GetDB())
}

// All returns all UserPushtoken records from the query.
func (q userPushtokenQuery) All(exec boil.Executor) (UserPushtokenSlice, error) {
	var o []*UserPushtoken

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to UserPushtoken slice")
	}

	if len(userPushtokenAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all UserPushtoken records in the query, and panics on error.
func (q userPushtokenQuery) CountG() (int64, error) {
	return q.Count(boil.GetDB())
}

// Count returns the count of all UserPushtoken records in the query.
func (q userPushtokenQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count user_pushtoken rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table, and panics on error.
func (q userPushtokenQuery) ExistsG() (bool, error) {
	return q.Exists(boil.GetDB())
}

// Exists checks if the row exists in the table.
func (q userPushtokenQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if user_pushtoken exists")
	}

	return count > 0, nil
}

// User pointed to by the foreign key.
func (o *UserPushtoken) User(mods ...qm.QueryMod) userProfileQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.UserID),
	}

	queryMods = append(queryMods, mods...)

	query := UserProfiles(queryMods...)
	queries.SetFrom(query.Query, "\"user_profile\"")

	return query
}

// LoadUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (userPushtokenL) LoadUser(e boil.Executor, singular bool, maybeUserPushtoken interface{}, mods queries.Applicator) error {
	var slice []*UserPushtoken
	var object *UserPushtoken

	if singular {
		object = maybeUserPushtoken.(*UserPushtoken)
	} else {
		slice = *maybeUserPushtoken.(*[]*UserPushtoken)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &userPushtokenR{}
		}
		args = append(args, object.UserID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userPushtokenR{}
			}

			for _, a := range args {
				if a == obj.UserID {
					continue Outer
				}
			}

			args = append(args, obj.UserID)

		}
	}

	query := NewQuery(qm.From(`user_profile`), qm.WhereIn(`id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load UserProfile")
	}

	var resultSlice []*UserProfile
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice UserProfile")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for user_profile")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for user_profile")
	}

	if len(userPushtokenAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.User = foreign
		if foreign.R == nil {
			foreign.R = &userProfileR{}
		}
		foreign.R.UserUserPushtokens = append(foreign.R.UserUserPushtokens, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UserID == foreign.ID {
				local.R.User = foreign
				if foreign.R == nil {
					foreign.R = &userProfileR{}
				}
				foreign.R.UserUserPushtokens = append(foreign.R.UserUserPushtokens, local)
				break
			}
		}
	}

	return nil
}

// SetUserG of the userPushtoken to the related item.
// Sets o.R.User to related.
// Adds o to related.R.UserUserPushtokens.
// Uses the global database handle.
func (o *UserPushtoken) SetUserG(insert bool, related *UserProfile) error {
	return o.SetUser(boil.GetDB(), insert, related)
}

// SetUser of the userPushtoken to the related item.
// Sets o.R.User to related.
// Adds o to related.R.UserUserPushtokens.
func (o *UserPushtoken) SetUser(exec boil.Executor, insert bool, related *UserProfile) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"user_pushtoken\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"user_id"}),
		strmangle.WhereClause("\"", "\"", 2, userPushtokenPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UserID = related.ID
	if o.R == nil {
		o.R = &userPushtokenR{
			User: related,
		}
	} else {
		o.R.User = related
	}

	if related.R == nil {
		related.R = &userProfileR{
			UserUserPushtokens: UserPushtokenSlice{o},
		}
	} else {
		related.R.UserUserPushtokens = append(related.R.UserUserPushtokens, o)
	}

	return nil
}

// UserPushtokens retrieves all the records using an executor.
func UserPushtokens(mods ...qm.QueryMod) userPushtokenQuery {
	mods = append(mods, qm.From("\"user_pushtoken\""))
	return userPushtokenQuery{NewQuery(mods...)}
}

// FindUserPushtokenG retrieves a single record by ID.
func FindUserPushtokenG(iD int, selectCols ...string) (*UserPushtoken, error) {
	return FindUserPushtoken(boil.GetDB(), iD, selectCols...)
}

// FindUserPushtoken retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindUserPushtoken(exec boil.Executor, iD int, selectCols ...string) (*UserPushtoken, error) {
	userPushtokenObj := &UserPushtoken{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"user_pushtoken\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, userPushtokenObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from user_pushtoken")
	}

	return userPushtokenObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *UserPushtoken) InsertG(columns boil.Columns) error {
	return o.Insert(boil.GetDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *UserPushtoken) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no user_pushtoken provided for insertion")
	}

	var err error
	currTime := time.Now().In(boil.GetLocation())

	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}
	if o.UpdatedAt.IsZero() {
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(userPushtokenColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	userPushtokenInsertCacheMut.RLock()
	cache, cached := userPushtokenInsertCache[key]
	userPushtokenInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			userPushtokenColumns,
			userPushtokenColumnsWithDefault,
			userPushtokenColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(userPushtokenType, userPushtokenMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(userPushtokenType, userPushtokenMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"user_pushtoken\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"user_pushtoken\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into user_pushtoken")
	}

	if !cached {
		userPushtokenInsertCacheMut.Lock()
		userPushtokenInsertCache[key] = cache
		userPushtokenInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single UserPushtoken record using the global executor.
// See Update for more documentation.
func (o *UserPushtoken) UpdateG(columns boil.Columns) (int64, error) {
	return o.Update(boil.GetDB(), columns)
}

// Update uses an executor to update the UserPushtoken.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *UserPushtoken) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	currTime := time.Now().In(boil.GetLocation())

	o.UpdatedAt = currTime

	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	userPushtokenUpdateCacheMut.RLock()
	cache, cached := userPushtokenUpdateCache[key]
	userPushtokenUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			userPushtokenColumns,
			userPushtokenPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update user_pushtoken, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"user_pushtoken\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, userPushtokenPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(userPushtokenType, userPushtokenMapping, append(wl, userPushtokenPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update user_pushtoken row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for user_pushtoken")
	}

	if !cached {
		userPushtokenUpdateCacheMut.Lock()
		userPushtokenUpdateCache[key] = cache
		userPushtokenUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(exec)
}

// UpdateAllG updates all rows with the specified column values.
func (q userPushtokenQuery) UpdateAllG(cols M) (int64, error) {
	return q.UpdateAll(boil.GetDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q userPushtokenQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for user_pushtoken")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for user_pushtoken")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o UserPushtokenSlice) UpdateAllG(cols M) (int64, error) {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o UserPushtokenSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userPushtokenPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"user_pushtoken\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, userPushtokenPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in userPushtoken slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all userPushtoken")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *UserPushtoken) UpsertG(updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	return o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *UserPushtoken) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no user_pushtoken provided for upsert")
	}
	currTime := time.Now().In(boil.GetLocation())

	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}
	o.UpdatedAt = currTime

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(userPushtokenColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	userPushtokenUpsertCacheMut.RLock()
	cache, cached := userPushtokenUpsertCache[key]
	userPushtokenUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			userPushtokenColumns,
			userPushtokenColumnsWithDefault,
			userPushtokenColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			userPushtokenColumns,
			userPushtokenPrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("models: unable to upsert user_pushtoken, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(userPushtokenPrimaryKeyColumns))
			copy(conflict, userPushtokenPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"user_pushtoken\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(userPushtokenType, userPushtokenMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(userPushtokenType, userPushtokenMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert user_pushtoken")
	}

	if !cached {
		userPushtokenUpsertCacheMut.Lock()
		userPushtokenUpsertCache[key] = cache
		userPushtokenUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteG deletes a single UserPushtoken record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *UserPushtoken) DeleteG() (int64, error) {
	return o.Delete(boil.GetDB())
}

// Delete deletes a single UserPushtoken record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *UserPushtoken) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no UserPushtoken provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), userPushtokenPrimaryKeyMapping)
	sql := "DELETE FROM \"user_pushtoken\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from user_pushtoken")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for user_pushtoken")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q userPushtokenQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no userPushtokenQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from user_pushtoken")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for user_pushtoken")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o UserPushtokenSlice) DeleteAllG() (int64, error) {
	return o.DeleteAll(boil.GetDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o UserPushtokenSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no UserPushtoken slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(userPushtokenBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userPushtokenPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"user_pushtoken\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, userPushtokenPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from userPushtoken slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for user_pushtoken")
	}

	if len(userPushtokenAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *UserPushtoken) ReloadG() error {
	if o == nil {
		return errors.New("models: no UserPushtoken provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *UserPushtoken) Reload(exec boil.Executor) error {
	ret, err := FindUserPushtoken(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *UserPushtokenSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("models: empty UserPushtokenSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *UserPushtokenSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := UserPushtokenSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userPushtokenPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"user_pushtoken\".* FROM \"user_pushtoken\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, userPushtokenPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in UserPushtokenSlice")
	}

	*o = slice

	return nil
}

// UserPushtokenExistsG checks if the UserPushtoken row exists.
func UserPushtokenExistsG(iD int) (bool, error) {
	return UserPushtokenExists(boil.GetDB(), iD)
}

// UserPushtokenExists checks if the UserPushtoken row exists.
func UserPushtokenExists(exec boil.Executor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"user_pushtoken\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}

	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if user_pushtoken exists")
	}

	return exists, nil
}
