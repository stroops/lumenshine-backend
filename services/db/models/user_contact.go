// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// UserContact is an object representing the database table.
type UserContact struct {
	ID             int       `boil:"id" json:"id" toml:"id" yaml:"id"`
	UserID         int       `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	ContactName    string    `boil:"contact_name" json:"contact_name" toml:"contact_name" yaml:"contact_name"`
	StellarAddress string    `boil:"stellar_address" json:"stellar_address" toml:"stellar_address" yaml:"stellar_address"`
	PublicKey      string    `boil:"public_key" json:"public_key" toml:"public_key" yaml:"public_key"`
	CreatedAt      time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt      time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	UpdatedBy      string    `boil:"updated_by" json:"updated_by" toml:"updated_by" yaml:"updated_by"`

	R *userContactR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L userContactL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var UserContactColumns = struct {
	ID             string
	UserID         string
	ContactName    string
	StellarAddress string
	PublicKey      string
	CreatedAt      string
	UpdatedAt      string
	UpdatedBy      string
}{
	ID:             "id",
	UserID:         "user_id",
	ContactName:    "contact_name",
	StellarAddress: "stellar_address",
	PublicKey:      "public_key",
	CreatedAt:      "created_at",
	UpdatedAt:      "updated_at",
	UpdatedBy:      "updated_by",
}

// UserContactRels is where relationship names are stored.
var UserContactRels = struct {
	User string
}{
	User: "User",
}

// userContactR is where relationships are stored.
type userContactR struct {
	User *UserProfile
}

// NewStruct creates a new relationship struct
func (*userContactR) NewStruct() *userContactR {
	return &userContactR{}
}

// userContactL is where Load methods for each relationship are stored.
type userContactL struct{}

var (
	userContactColumns               = []string{"id", "user_id", "contact_name", "stellar_address", "public_key", "created_at", "updated_at", "updated_by"}
	userContactColumnsWithoutDefault = []string{"user_id", "contact_name", "stellar_address", "public_key", "updated_by"}
	userContactColumnsWithDefault    = []string{"id", "created_at", "updated_at"}
	userContactPrimaryKeyColumns     = []string{"id"}
)

type (
	// UserContactSlice is an alias for a slice of pointers to UserContact.
	// This should generally be used opposed to []UserContact.
	UserContactSlice []*UserContact
	// UserContactHook is the signature for custom UserContact hook methods
	UserContactHook func(boil.Executor, *UserContact) error

	userContactQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	userContactType                 = reflect.TypeOf(&UserContact{})
	userContactMapping              = queries.MakeStructMapping(userContactType)
	userContactPrimaryKeyMapping, _ = queries.BindMapping(userContactType, userContactMapping, userContactPrimaryKeyColumns)
	userContactInsertCacheMut       sync.RWMutex
	userContactInsertCache          = make(map[string]insertCache)
	userContactUpdateCacheMut       sync.RWMutex
	userContactUpdateCache          = make(map[string]updateCache)
	userContactUpsertCacheMut       sync.RWMutex
	userContactUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
)

var userContactBeforeInsertHooks []UserContactHook
var userContactBeforeUpdateHooks []UserContactHook
var userContactBeforeDeleteHooks []UserContactHook
var userContactBeforeUpsertHooks []UserContactHook

var userContactAfterInsertHooks []UserContactHook
var userContactAfterSelectHooks []UserContactHook
var userContactAfterUpdateHooks []UserContactHook
var userContactAfterDeleteHooks []UserContactHook
var userContactAfterUpsertHooks []UserContactHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *UserContact) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range userContactBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *UserContact) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range userContactBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *UserContact) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range userContactBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *UserContact) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range userContactBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *UserContact) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range userContactAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *UserContact) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range userContactAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *UserContact) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range userContactAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *UserContact) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range userContactAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *UserContact) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range userContactAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddUserContactHook registers your hook function for all future operations.
func AddUserContactHook(hookPoint boil.HookPoint, userContactHook UserContactHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		userContactBeforeInsertHooks = append(userContactBeforeInsertHooks, userContactHook)
	case boil.BeforeUpdateHook:
		userContactBeforeUpdateHooks = append(userContactBeforeUpdateHooks, userContactHook)
	case boil.BeforeDeleteHook:
		userContactBeforeDeleteHooks = append(userContactBeforeDeleteHooks, userContactHook)
	case boil.BeforeUpsertHook:
		userContactBeforeUpsertHooks = append(userContactBeforeUpsertHooks, userContactHook)
	case boil.AfterInsertHook:
		userContactAfterInsertHooks = append(userContactAfterInsertHooks, userContactHook)
	case boil.AfterSelectHook:
		userContactAfterSelectHooks = append(userContactAfterSelectHooks, userContactHook)
	case boil.AfterUpdateHook:
		userContactAfterUpdateHooks = append(userContactAfterUpdateHooks, userContactHook)
	case boil.AfterDeleteHook:
		userContactAfterDeleteHooks = append(userContactAfterDeleteHooks, userContactHook)
	case boil.AfterUpsertHook:
		userContactAfterUpsertHooks = append(userContactAfterUpsertHooks, userContactHook)
	}
}

// OneG returns a single userContact record from the query using the global executor.
func (q userContactQuery) OneG() (*UserContact, error) {
	return q.One(boil.GetDB())
}

// One returns a single userContact record from the query.
func (q userContactQuery) One(exec boil.Executor) (*UserContact, error) {
	o := &UserContact{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for user_contact")
	}

	if err := o.doAfterSelectHooks(exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all UserContact records from the query using the global executor.
func (q userContactQuery) AllG() (UserContactSlice, error) {
	return q.All(boil.GetDB())
}

// All returns all UserContact records from the query.
func (q userContactQuery) All(exec boil.Executor) (UserContactSlice, error) {
	var o []*UserContact

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to UserContact slice")
	}

	if len(userContactAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all UserContact records in the query, and panics on error.
func (q userContactQuery) CountG() (int64, error) {
	return q.Count(boil.GetDB())
}

// Count returns the count of all UserContact records in the query.
func (q userContactQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count user_contact rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table, and panics on error.
func (q userContactQuery) ExistsG() (bool, error) {
	return q.Exists(boil.GetDB())
}

// Exists checks if the row exists in the table.
func (q userContactQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if user_contact exists")
	}

	return count > 0, nil
}

// User pointed to by the foreign key.
func (o *UserContact) User(mods ...qm.QueryMod) userProfileQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.UserID),
	}

	queryMods = append(queryMods, mods...)

	query := UserProfiles(queryMods...)
	queries.SetFrom(query.Query, "\"user_profile\"")

	return query
}

// LoadUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (userContactL) LoadUser(e boil.Executor, singular bool, maybeUserContact interface{}, mods queries.Applicator) error {
	var slice []*UserContact
	var object *UserContact

	if singular {
		object = maybeUserContact.(*UserContact)
	} else {
		slice = *maybeUserContact.(*[]*UserContact)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &userContactR{}
		}
		args = append(args, object.UserID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userContactR{}
			}

			for _, a := range args {
				if a == obj.UserID {
					continue Outer
				}
			}

			args = append(args, obj.UserID)

		}
	}

	query := NewQuery(qm.From(`user_profile`), qm.WhereIn(`id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load UserProfile")
	}

	var resultSlice []*UserProfile
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice UserProfile")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for user_profile")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for user_profile")
	}

	if len(userContactAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.User = foreign
		if foreign.R == nil {
			foreign.R = &userProfileR{}
		}
		foreign.R.UserUserContacts = append(foreign.R.UserUserContacts, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UserID == foreign.ID {
				local.R.User = foreign
				if foreign.R == nil {
					foreign.R = &userProfileR{}
				}
				foreign.R.UserUserContacts = append(foreign.R.UserUserContacts, local)
				break
			}
		}
	}

	return nil
}

// SetUserG of the userContact to the related item.
// Sets o.R.User to related.
// Adds o to related.R.UserUserContacts.
// Uses the global database handle.
func (o *UserContact) SetUserG(insert bool, related *UserProfile) error {
	return o.SetUser(boil.GetDB(), insert, related)
}

// SetUser of the userContact to the related item.
// Sets o.R.User to related.
// Adds o to related.R.UserUserContacts.
func (o *UserContact) SetUser(exec boil.Executor, insert bool, related *UserProfile) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"user_contact\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"user_id"}),
		strmangle.WhereClause("\"", "\"", 2, userContactPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UserID = related.ID
	if o.R == nil {
		o.R = &userContactR{
			User: related,
		}
	} else {
		o.R.User = related
	}

	if related.R == nil {
		related.R = &userProfileR{
			UserUserContacts: UserContactSlice{o},
		}
	} else {
		related.R.UserUserContacts = append(related.R.UserUserContacts, o)
	}

	return nil
}

// UserContacts retrieves all the records using an executor.
func UserContacts(mods ...qm.QueryMod) userContactQuery {
	mods = append(mods, qm.From("\"user_contact\""))
	return userContactQuery{NewQuery(mods...)}
}

// FindUserContactG retrieves a single record by ID.
func FindUserContactG(iD int, selectCols ...string) (*UserContact, error) {
	return FindUserContact(boil.GetDB(), iD, selectCols...)
}

// FindUserContact retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindUserContact(exec boil.Executor, iD int, selectCols ...string) (*UserContact, error) {
	userContactObj := &UserContact{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"user_contact\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, userContactObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from user_contact")
	}

	return userContactObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *UserContact) InsertG(columns boil.Columns) error {
	return o.Insert(boil.GetDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *UserContact) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no user_contact provided for insertion")
	}

	var err error
	currTime := time.Now().In(boil.GetLocation())

	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}
	if o.UpdatedAt.IsZero() {
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(userContactColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	userContactInsertCacheMut.RLock()
	cache, cached := userContactInsertCache[key]
	userContactInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			userContactColumns,
			userContactColumnsWithDefault,
			userContactColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(userContactType, userContactMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(userContactType, userContactMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"user_contact\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"user_contact\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into user_contact")
	}

	if !cached {
		userContactInsertCacheMut.Lock()
		userContactInsertCache[key] = cache
		userContactInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single UserContact record using the global executor.
// See Update for more documentation.
func (o *UserContact) UpdateG(columns boil.Columns) (int64, error) {
	return o.Update(boil.GetDB(), columns)
}

// Update uses an executor to update the UserContact.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *UserContact) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	currTime := time.Now().In(boil.GetLocation())

	o.UpdatedAt = currTime

	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	userContactUpdateCacheMut.RLock()
	cache, cached := userContactUpdateCache[key]
	userContactUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			userContactColumns,
			userContactPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update user_contact, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"user_contact\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, userContactPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(userContactType, userContactMapping, append(wl, userContactPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update user_contact row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for user_contact")
	}

	if !cached {
		userContactUpdateCacheMut.Lock()
		userContactUpdateCache[key] = cache
		userContactUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(exec)
}

// UpdateAllG updates all rows with the specified column values.
func (q userContactQuery) UpdateAllG(cols M) (int64, error) {
	return q.UpdateAll(boil.GetDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q userContactQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for user_contact")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for user_contact")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o UserContactSlice) UpdateAllG(cols M) (int64, error) {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o UserContactSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userContactPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"user_contact\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, userContactPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in userContact slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all userContact")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *UserContact) UpsertG(updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	return o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *UserContact) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no user_contact provided for upsert")
	}
	currTime := time.Now().In(boil.GetLocation())

	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}
	o.UpdatedAt = currTime

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(userContactColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	userContactUpsertCacheMut.RLock()
	cache, cached := userContactUpsertCache[key]
	userContactUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			userContactColumns,
			userContactColumnsWithDefault,
			userContactColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			userContactColumns,
			userContactPrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("models: unable to upsert user_contact, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(userContactPrimaryKeyColumns))
			copy(conflict, userContactPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"user_contact\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(userContactType, userContactMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(userContactType, userContactMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert user_contact")
	}

	if !cached {
		userContactUpsertCacheMut.Lock()
		userContactUpsertCache[key] = cache
		userContactUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteG deletes a single UserContact record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *UserContact) DeleteG() (int64, error) {
	return o.Delete(boil.GetDB())
}

// Delete deletes a single UserContact record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *UserContact) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no UserContact provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), userContactPrimaryKeyMapping)
	sql := "DELETE FROM \"user_contact\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from user_contact")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for user_contact")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q userContactQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no userContactQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from user_contact")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for user_contact")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o UserContactSlice) DeleteAllG() (int64, error) {
	return o.DeleteAll(boil.GetDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o UserContactSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no UserContact slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(userContactBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userContactPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"user_contact\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, userContactPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from userContact slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for user_contact")
	}

	if len(userContactAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *UserContact) ReloadG() error {
	if o == nil {
		return errors.New("models: no UserContact provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *UserContact) Reload(exec boil.Executor) error {
	ret, err := FindUserContact(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *UserContactSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("models: empty UserContactSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *UserContactSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := UserContactSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userContactPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"user_contact\".* FROM \"user_contact\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, userContactPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in UserContactSlice")
	}

	*o = slice

	return nil
}

// UserContactExistsG checks if the UserContact row exists.
func UserContactExistsG(iD int) (bool, error) {
	return UserContactExists(boil.GetDB(), iD)
}

// UserContactExists checks if the UserContact row exists.
func UserContactExists(exec boil.Executor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"user_contact\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}

	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if user_contact exists")
	}

	return exists, nil
}
