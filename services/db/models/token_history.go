// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// TokenHistory is an object representing the database table.
type TokenHistory struct {
	ID                  int       `boil:"id" json:"id" toml:"id" yaml:"id"`
	UserID              int       `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	MailConfirmationKey string    `boil:"mail_confirmation_key" json:"mail_confirmation_key" toml:"mail_confirmation_key" yaml:"mail_confirmation_key"`
	CreatedAt           time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *tokenHistoryR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L tokenHistoryL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TokenHistoryColumns = struct {
	ID                  string
	UserID              string
	MailConfirmationKey string
	CreatedAt           string
}{
	ID:                  "id",
	UserID:              "user_id",
	MailConfirmationKey: "mail_confirmation_key",
	CreatedAt:           "created_at",
}

// TokenHistoryRels is where relationship names are stored.
var TokenHistoryRels = struct {
	User string
}{
	User: "User",
}

// tokenHistoryR is where relationships are stored.
type tokenHistoryR struct {
	User *UserProfile
}

// NewStruct creates a new relationship struct
func (*tokenHistoryR) NewStruct() *tokenHistoryR {
	return &tokenHistoryR{}
}

// tokenHistoryL is where Load methods for each relationship are stored.
type tokenHistoryL struct{}

var (
	tokenHistoryColumns               = []string{"id", "user_id", "mail_confirmation_key", "created_at"}
	tokenHistoryColumnsWithoutDefault = []string{"user_id", "mail_confirmation_key"}
	tokenHistoryColumnsWithDefault    = []string{"id", "created_at"}
	tokenHistoryPrimaryKeyColumns     = []string{"id"}
)

type (
	// TokenHistorySlice is an alias for a slice of pointers to TokenHistory.
	// This should generally be used opposed to []TokenHistory.
	TokenHistorySlice []*TokenHistory
	// TokenHistoryHook is the signature for custom TokenHistory hook methods
	TokenHistoryHook func(boil.Executor, *TokenHistory) error

	tokenHistoryQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	tokenHistoryType                 = reflect.TypeOf(&TokenHistory{})
	tokenHistoryMapping              = queries.MakeStructMapping(tokenHistoryType)
	tokenHistoryPrimaryKeyMapping, _ = queries.BindMapping(tokenHistoryType, tokenHistoryMapping, tokenHistoryPrimaryKeyColumns)
	tokenHistoryInsertCacheMut       sync.RWMutex
	tokenHistoryInsertCache          = make(map[string]insertCache)
	tokenHistoryUpdateCacheMut       sync.RWMutex
	tokenHistoryUpdateCache          = make(map[string]updateCache)
	tokenHistoryUpsertCacheMut       sync.RWMutex
	tokenHistoryUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
)

var tokenHistoryBeforeInsertHooks []TokenHistoryHook
var tokenHistoryBeforeUpdateHooks []TokenHistoryHook
var tokenHistoryBeforeDeleteHooks []TokenHistoryHook
var tokenHistoryBeforeUpsertHooks []TokenHistoryHook

var tokenHistoryAfterInsertHooks []TokenHistoryHook
var tokenHistoryAfterSelectHooks []TokenHistoryHook
var tokenHistoryAfterUpdateHooks []TokenHistoryHook
var tokenHistoryAfterDeleteHooks []TokenHistoryHook
var tokenHistoryAfterUpsertHooks []TokenHistoryHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TokenHistory) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range tokenHistoryBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TokenHistory) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range tokenHistoryBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TokenHistory) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range tokenHistoryBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TokenHistory) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range tokenHistoryBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TokenHistory) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range tokenHistoryAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TokenHistory) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range tokenHistoryAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TokenHistory) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range tokenHistoryAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TokenHistory) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range tokenHistoryAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TokenHistory) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range tokenHistoryAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTokenHistoryHook registers your hook function for all future operations.
func AddTokenHistoryHook(hookPoint boil.HookPoint, tokenHistoryHook TokenHistoryHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		tokenHistoryBeforeInsertHooks = append(tokenHistoryBeforeInsertHooks, tokenHistoryHook)
	case boil.BeforeUpdateHook:
		tokenHistoryBeforeUpdateHooks = append(tokenHistoryBeforeUpdateHooks, tokenHistoryHook)
	case boil.BeforeDeleteHook:
		tokenHistoryBeforeDeleteHooks = append(tokenHistoryBeforeDeleteHooks, tokenHistoryHook)
	case boil.BeforeUpsertHook:
		tokenHistoryBeforeUpsertHooks = append(tokenHistoryBeforeUpsertHooks, tokenHistoryHook)
	case boil.AfterInsertHook:
		tokenHistoryAfterInsertHooks = append(tokenHistoryAfterInsertHooks, tokenHistoryHook)
	case boil.AfterSelectHook:
		tokenHistoryAfterSelectHooks = append(tokenHistoryAfterSelectHooks, tokenHistoryHook)
	case boil.AfterUpdateHook:
		tokenHistoryAfterUpdateHooks = append(tokenHistoryAfterUpdateHooks, tokenHistoryHook)
	case boil.AfterDeleteHook:
		tokenHistoryAfterDeleteHooks = append(tokenHistoryAfterDeleteHooks, tokenHistoryHook)
	case boil.AfterUpsertHook:
		tokenHistoryAfterUpsertHooks = append(tokenHistoryAfterUpsertHooks, tokenHistoryHook)
	}
}

// OneG returns a single tokenHistory record from the query using the global executor.
func (q tokenHistoryQuery) OneG() (*TokenHistory, error) {
	return q.One(boil.GetDB())
}

// One returns a single tokenHistory record from the query.
func (q tokenHistoryQuery) One(exec boil.Executor) (*TokenHistory, error) {
	o := &TokenHistory{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for token_history")
	}

	if err := o.doAfterSelectHooks(exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all TokenHistory records from the query using the global executor.
func (q tokenHistoryQuery) AllG() (TokenHistorySlice, error) {
	return q.All(boil.GetDB())
}

// All returns all TokenHistory records from the query.
func (q tokenHistoryQuery) All(exec boil.Executor) (TokenHistorySlice, error) {
	var o []*TokenHistory

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TokenHistory slice")
	}

	if len(tokenHistoryAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all TokenHistory records in the query, and panics on error.
func (q tokenHistoryQuery) CountG() (int64, error) {
	return q.Count(boil.GetDB())
}

// Count returns the count of all TokenHistory records in the query.
func (q tokenHistoryQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count token_history rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table, and panics on error.
func (q tokenHistoryQuery) ExistsG() (bool, error) {
	return q.Exists(boil.GetDB())
}

// Exists checks if the row exists in the table.
func (q tokenHistoryQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if token_history exists")
	}

	return count > 0, nil
}

// User pointed to by the foreign key.
func (o *TokenHistory) User(mods ...qm.QueryMod) userProfileQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.UserID),
	}

	queryMods = append(queryMods, mods...)

	query := UserProfiles(queryMods...)
	queries.SetFrom(query.Query, "\"user_profile\"")

	return query
}

// LoadUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (tokenHistoryL) LoadUser(e boil.Executor, singular bool, maybeTokenHistory interface{}, mods queries.Applicator) error {
	var slice []*TokenHistory
	var object *TokenHistory

	if singular {
		object = maybeTokenHistory.(*TokenHistory)
	} else {
		slice = *maybeTokenHistory.(*[]*TokenHistory)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &tokenHistoryR{}
		}
		args = append(args, object.UserID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &tokenHistoryR{}
			}

			for _, a := range args {
				if a == obj.UserID {
					continue Outer
				}
			}

			args = append(args, obj.UserID)

		}
	}

	query := NewQuery(qm.From(`user_profile`), qm.WhereIn(`id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load UserProfile")
	}

	var resultSlice []*UserProfile
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice UserProfile")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for user_profile")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for user_profile")
	}

	if len(tokenHistoryAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.User = foreign
		if foreign.R == nil {
			foreign.R = &userProfileR{}
		}
		foreign.R.UserTokenHistories = append(foreign.R.UserTokenHistories, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UserID == foreign.ID {
				local.R.User = foreign
				if foreign.R == nil {
					foreign.R = &userProfileR{}
				}
				foreign.R.UserTokenHistories = append(foreign.R.UserTokenHistories, local)
				break
			}
		}
	}

	return nil
}

// SetUserG of the tokenHistory to the related item.
// Sets o.R.User to related.
// Adds o to related.R.UserTokenHistories.
// Uses the global database handle.
func (o *TokenHistory) SetUserG(insert bool, related *UserProfile) error {
	return o.SetUser(boil.GetDB(), insert, related)
}

// SetUser of the tokenHistory to the related item.
// Sets o.R.User to related.
// Adds o to related.R.UserTokenHistories.
func (o *TokenHistory) SetUser(exec boil.Executor, insert bool, related *UserProfile) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"token_history\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"user_id"}),
		strmangle.WhereClause("\"", "\"", 2, tokenHistoryPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UserID = related.ID
	if o.R == nil {
		o.R = &tokenHistoryR{
			User: related,
		}
	} else {
		o.R.User = related
	}

	if related.R == nil {
		related.R = &userProfileR{
			UserTokenHistories: TokenHistorySlice{o},
		}
	} else {
		related.R.UserTokenHistories = append(related.R.UserTokenHistories, o)
	}

	return nil
}

// TokenHistories retrieves all the records using an executor.
func TokenHistories(mods ...qm.QueryMod) tokenHistoryQuery {
	mods = append(mods, qm.From("\"token_history\""))
	return tokenHistoryQuery{NewQuery(mods...)}
}

// FindTokenHistoryG retrieves a single record by ID.
func FindTokenHistoryG(iD int, selectCols ...string) (*TokenHistory, error) {
	return FindTokenHistory(boil.GetDB(), iD, selectCols...)
}

// FindTokenHistory retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTokenHistory(exec boil.Executor, iD int, selectCols ...string) (*TokenHistory, error) {
	tokenHistoryObj := &TokenHistory{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"token_history\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, tokenHistoryObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from token_history")
	}

	return tokenHistoryObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *TokenHistory) InsertG(columns boil.Columns) error {
	return o.Insert(boil.GetDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TokenHistory) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no token_history provided for insertion")
	}

	var err error
	currTime := time.Now().In(boil.GetLocation())

	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tokenHistoryColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	tokenHistoryInsertCacheMut.RLock()
	cache, cached := tokenHistoryInsertCache[key]
	tokenHistoryInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			tokenHistoryColumns,
			tokenHistoryColumnsWithDefault,
			tokenHistoryColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(tokenHistoryType, tokenHistoryMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(tokenHistoryType, tokenHistoryMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"token_history\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"token_history\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into token_history")
	}

	if !cached {
		tokenHistoryInsertCacheMut.Lock()
		tokenHistoryInsertCache[key] = cache
		tokenHistoryInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single TokenHistory record using the global executor.
// See Update for more documentation.
func (o *TokenHistory) UpdateG(columns boil.Columns) (int64, error) {
	return o.Update(boil.GetDB(), columns)
}

// Update uses an executor to update the TokenHistory.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TokenHistory) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	tokenHistoryUpdateCacheMut.RLock()
	cache, cached := tokenHistoryUpdateCache[key]
	tokenHistoryUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			tokenHistoryColumns,
			tokenHistoryPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update token_history, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"token_history\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, tokenHistoryPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(tokenHistoryType, tokenHistoryMapping, append(wl, tokenHistoryPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update token_history row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for token_history")
	}

	if !cached {
		tokenHistoryUpdateCacheMut.Lock()
		tokenHistoryUpdateCache[key] = cache
		tokenHistoryUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(exec)
}

// UpdateAllG updates all rows with the specified column values.
func (q tokenHistoryQuery) UpdateAllG(cols M) (int64, error) {
	return q.UpdateAll(boil.GetDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q tokenHistoryQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for token_history")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for token_history")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o TokenHistorySlice) UpdateAllG(cols M) (int64, error) {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TokenHistorySlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tokenHistoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"token_history\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, tokenHistoryPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in tokenHistory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all tokenHistory")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *TokenHistory) UpsertG(updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	return o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TokenHistory) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no token_history provided for upsert")
	}
	currTime := time.Now().In(boil.GetLocation())

	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tokenHistoryColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	tokenHistoryUpsertCacheMut.RLock()
	cache, cached := tokenHistoryUpsertCache[key]
	tokenHistoryUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			tokenHistoryColumns,
			tokenHistoryColumnsWithDefault,
			tokenHistoryColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			tokenHistoryColumns,
			tokenHistoryPrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("models: unable to upsert token_history, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(tokenHistoryPrimaryKeyColumns))
			copy(conflict, tokenHistoryPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"token_history\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(tokenHistoryType, tokenHistoryMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(tokenHistoryType, tokenHistoryMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert token_history")
	}

	if !cached {
		tokenHistoryUpsertCacheMut.Lock()
		tokenHistoryUpsertCache[key] = cache
		tokenHistoryUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteG deletes a single TokenHistory record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *TokenHistory) DeleteG() (int64, error) {
	return o.Delete(boil.GetDB())
}

// Delete deletes a single TokenHistory record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TokenHistory) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TokenHistory provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), tokenHistoryPrimaryKeyMapping)
	sql := "DELETE FROM \"token_history\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from token_history")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for token_history")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q tokenHistoryQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no tokenHistoryQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from token_history")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for token_history")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o TokenHistorySlice) DeleteAllG() (int64, error) {
	return o.DeleteAll(boil.GetDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TokenHistorySlice) DeleteAll(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TokenHistory slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(tokenHistoryBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tokenHistoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"token_history\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, tokenHistoryPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from tokenHistory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for token_history")
	}

	if len(tokenHistoryAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *TokenHistory) ReloadG() error {
	if o == nil {
		return errors.New("models: no TokenHistory provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TokenHistory) Reload(exec boil.Executor) error {
	ret, err := FindTokenHistory(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TokenHistorySlice) ReloadAllG() error {
	if o == nil {
		return errors.New("models: empty TokenHistorySlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TokenHistorySlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TokenHistorySlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tokenHistoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"token_history\".* FROM \"token_history\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, tokenHistoryPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TokenHistorySlice")
	}

	*o = slice

	return nil
}

// TokenHistoryExistsG checks if the TokenHistory row exists.
func TokenHistoryExistsG(iD int) (bool, error) {
	return TokenHistoryExists(boil.GetDB(), iD)
}

// TokenHistoryExists checks if the TokenHistory row exists.
func TokenHistoryExists(exec boil.Executor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"token_history\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}

	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if token_history exists")
	}

	return exists, nil
}
