// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package stellarcore

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// Pubsub is an object representing the database table.
type Pubsub struct {
	Resid    string   `boil:"resid" json:"resid" toml:"resid" yaml:"resid"`
	Lastread null.Int `boil:"lastread" json:"lastread,omitempty" toml:"lastread" yaml:"lastread,omitempty"`

	R *pubsubR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L pubsubL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var PubsubColumns = struct {
	Resid    string
	Lastread string
}{
	Resid:    "resid",
	Lastread: "lastread",
}

// PubsubRels is where relationship names are stored.
var PubsubRels = struct {
}{}

// pubsubR is where relationships are stored.
type pubsubR struct {
}

// NewStruct creates a new relationship struct
func (*pubsubR) NewStruct() *pubsubR {
	return &pubsubR{}
}

// pubsubL is where Load methods for each relationship are stored.
type pubsubL struct{}

var (
	pubsubColumns               = []string{"resid", "lastread"}
	pubsubColumnsWithoutDefault = []string{"resid", "lastread"}
	pubsubColumnsWithDefault    = []string{}
	pubsubPrimaryKeyColumns     = []string{"resid"}
)

type (
	// PubsubSlice is an alias for a slice of pointers to Pubsub.
	// This should generally be used opposed to []Pubsub.
	PubsubSlice []*Pubsub
	// PubsubHook is the signature for custom Pubsub hook methods
	PubsubHook func(boil.Executor, *Pubsub) error

	pubsubQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	pubsubType                 = reflect.TypeOf(&Pubsub{})
	pubsubMapping              = queries.MakeStructMapping(pubsubType)
	pubsubPrimaryKeyMapping, _ = queries.BindMapping(pubsubType, pubsubMapping, pubsubPrimaryKeyColumns)
	pubsubInsertCacheMut       sync.RWMutex
	pubsubInsertCache          = make(map[string]insertCache)
	pubsubUpdateCacheMut       sync.RWMutex
	pubsubUpdateCache          = make(map[string]updateCache)
	pubsubUpsertCacheMut       sync.RWMutex
	pubsubUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
)

var pubsubBeforeInsertHooks []PubsubHook
var pubsubBeforeUpdateHooks []PubsubHook
var pubsubBeforeDeleteHooks []PubsubHook
var pubsubBeforeUpsertHooks []PubsubHook

var pubsubAfterInsertHooks []PubsubHook
var pubsubAfterSelectHooks []PubsubHook
var pubsubAfterUpdateHooks []PubsubHook
var pubsubAfterDeleteHooks []PubsubHook
var pubsubAfterUpsertHooks []PubsubHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *Pubsub) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range pubsubBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *Pubsub) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range pubsubBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *Pubsub) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range pubsubBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *Pubsub) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range pubsubBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *Pubsub) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range pubsubAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *Pubsub) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range pubsubAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *Pubsub) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range pubsubAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *Pubsub) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range pubsubAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *Pubsub) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range pubsubAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddPubsubHook registers your hook function for all future operations.
func AddPubsubHook(hookPoint boil.HookPoint, pubsubHook PubsubHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		pubsubBeforeInsertHooks = append(pubsubBeforeInsertHooks, pubsubHook)
	case boil.BeforeUpdateHook:
		pubsubBeforeUpdateHooks = append(pubsubBeforeUpdateHooks, pubsubHook)
	case boil.BeforeDeleteHook:
		pubsubBeforeDeleteHooks = append(pubsubBeforeDeleteHooks, pubsubHook)
	case boil.BeforeUpsertHook:
		pubsubBeforeUpsertHooks = append(pubsubBeforeUpsertHooks, pubsubHook)
	case boil.AfterInsertHook:
		pubsubAfterInsertHooks = append(pubsubAfterInsertHooks, pubsubHook)
	case boil.AfterSelectHook:
		pubsubAfterSelectHooks = append(pubsubAfterSelectHooks, pubsubHook)
	case boil.AfterUpdateHook:
		pubsubAfterUpdateHooks = append(pubsubAfterUpdateHooks, pubsubHook)
	case boil.AfterDeleteHook:
		pubsubAfterDeleteHooks = append(pubsubAfterDeleteHooks, pubsubHook)
	case boil.AfterUpsertHook:
		pubsubAfterUpsertHooks = append(pubsubAfterUpsertHooks, pubsubHook)
	}
}

// OneG returns a single pubsub record from the query using the global executor.
func (q pubsubQuery) OneG() (*Pubsub, error) {
	return q.One(boil.GetDB())
}

// One returns a single pubsub record from the query.
func (q pubsubQuery) One(exec boil.Executor) (*Pubsub, error) {
	o := &Pubsub{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "stellarcore: failed to execute a one query for pubsub")
	}

	if err := o.doAfterSelectHooks(exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all Pubsub records from the query using the global executor.
func (q pubsubQuery) AllG() (PubsubSlice, error) {
	return q.All(boil.GetDB())
}

// All returns all Pubsub records from the query.
func (q pubsubQuery) All(exec boil.Executor) (PubsubSlice, error) {
	var o []*Pubsub

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "stellarcore: failed to assign all query results to Pubsub slice")
	}

	if len(pubsubAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all Pubsub records in the query, and panics on error.
func (q pubsubQuery) CountG() (int64, error) {
	return q.Count(boil.GetDB())
}

// Count returns the count of all Pubsub records in the query.
func (q pubsubQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: failed to count pubsub rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table, and panics on error.
func (q pubsubQuery) ExistsG() (bool, error) {
	return q.Exists(boil.GetDB())
}

// Exists checks if the row exists in the table.
func (q pubsubQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "stellarcore: failed to check if pubsub exists")
	}

	return count > 0, nil
}

// Pubsubs retrieves all the records using an executor.
func Pubsubs(mods ...qm.QueryMod) pubsubQuery {
	mods = append(mods, qm.From("\"pubsub\""))
	return pubsubQuery{NewQuery(mods...)}
}

// FindPubsubG retrieves a single record by ID.
func FindPubsubG(resid string, selectCols ...string) (*Pubsub, error) {
	return FindPubsub(boil.GetDB(), resid, selectCols...)
}

// FindPubsub retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindPubsub(exec boil.Executor, resid string, selectCols ...string) (*Pubsub, error) {
	pubsubObj := &Pubsub{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"pubsub\" where \"resid\"=$1", sel,
	)

	q := queries.Raw(query, resid)

	err := q.Bind(nil, exec, pubsubObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "stellarcore: unable to select from pubsub")
	}

	return pubsubObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *Pubsub) InsertG(columns boil.Columns) error {
	return o.Insert(boil.GetDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Pubsub) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("stellarcore: no pubsub provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(pubsubColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	pubsubInsertCacheMut.RLock()
	cache, cached := pubsubInsertCache[key]
	pubsubInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			pubsubColumns,
			pubsubColumnsWithDefault,
			pubsubColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(pubsubType, pubsubMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(pubsubType, pubsubMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"pubsub\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"pubsub\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "stellarcore: unable to insert into pubsub")
	}

	if !cached {
		pubsubInsertCacheMut.Lock()
		pubsubInsertCache[key] = cache
		pubsubInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single Pubsub record using the global executor.
// See Update for more documentation.
func (o *Pubsub) UpdateG(columns boil.Columns) (int64, error) {
	return o.Update(boil.GetDB(), columns)
}

// Update uses an executor to update the Pubsub.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Pubsub) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	pubsubUpdateCacheMut.RLock()
	cache, cached := pubsubUpdateCache[key]
	pubsubUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			pubsubColumns,
			pubsubPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("stellarcore: unable to update pubsub, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"pubsub\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, pubsubPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(pubsubType, pubsubMapping, append(wl, pubsubPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to update pubsub row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: failed to get rows affected by update for pubsub")
	}

	if !cached {
		pubsubUpdateCacheMut.Lock()
		pubsubUpdateCache[key] = cache
		pubsubUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(exec)
}

// UpdateAll updates all rows with the specified column values.
func (q pubsubQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to update all for pubsub")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to retrieve rows affected for pubsub")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o PubsubSlice) UpdateAllG(cols M) (int64, error) {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o PubsubSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("stellarcore: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), pubsubPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"pubsub\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, pubsubPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to update all in pubsub slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to retrieve rows affected all in update all pubsub")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *Pubsub) UpsertG(updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	return o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Pubsub) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("stellarcore: no pubsub provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(pubsubColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	pubsubUpsertCacheMut.RLock()
	cache, cached := pubsubUpsertCache[key]
	pubsubUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			pubsubColumns,
			pubsubColumnsWithDefault,
			pubsubColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			pubsubColumns,
			pubsubPrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("stellarcore: unable to upsert pubsub, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(pubsubPrimaryKeyColumns))
			copy(conflict, pubsubPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"pubsub\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(pubsubType, pubsubMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(pubsubType, pubsubMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "stellarcore: unable to upsert pubsub")
	}

	if !cached {
		pubsubUpsertCacheMut.Lock()
		pubsubUpsertCache[key] = cache
		pubsubUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteG deletes a single Pubsub record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *Pubsub) DeleteG() (int64, error) {
	return o.Delete(boil.GetDB())
}

// Delete deletes a single Pubsub record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Pubsub) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("stellarcore: no Pubsub provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), pubsubPrimaryKeyMapping)
	sql := "DELETE FROM \"pubsub\" WHERE \"resid\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to delete from pubsub")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: failed to get rows affected by delete for pubsub")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q pubsubQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("stellarcore: no pubsubQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to delete all from pubsub")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: failed to get rows affected by deleteall for pubsub")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o PubsubSlice) DeleteAllG() (int64, error) {
	return o.DeleteAll(boil.GetDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o PubsubSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("stellarcore: no Pubsub slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(pubsubBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), pubsubPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"pubsub\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, pubsubPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to delete all from pubsub slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: failed to get rows affected by deleteall for pubsub")
	}

	if len(pubsubAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *Pubsub) ReloadG() error {
	if o == nil {
		return errors.New("stellarcore: no Pubsub provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Pubsub) Reload(exec boil.Executor) error {
	ret, err := FindPubsub(exec, o.Resid)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PubsubSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("stellarcore: empty PubsubSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PubsubSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := PubsubSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), pubsubPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"pubsub\".* FROM \"pubsub\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, pubsubPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "stellarcore: unable to reload all in PubsubSlice")
	}

	*o = slice

	return nil
}

// PubsubExistsG checks if the Pubsub row exists.
func PubsubExistsG(resid string) (bool, error) {
	return PubsubExists(boil.GetDB(), resid)
}

// PubsubExists checks if the Pubsub row exists.
func PubsubExists(exec boil.Executor, resid string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"pubsub\" where \"resid\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, resid)
	}

	row := exec.QueryRow(sql, resid)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "stellarcore: unable to check if pubsub exists")
	}

	return exists, nil
}
