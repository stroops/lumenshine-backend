// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package stellarcore

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// Ban is an object representing the database table.
type Ban struct {
	Nodeid string `boil:"nodeid" json:"nodeid" toml:"nodeid" yaml:"nodeid"`

	R *banR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L banL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var BanColumns = struct {
	Nodeid string
}{
	Nodeid: "nodeid",
}

// BanRels is where relationship names are stored.
var BanRels = struct {
}{}

// banR is where relationships are stored.
type banR struct {
}

// NewStruct creates a new relationship struct
func (*banR) NewStruct() *banR {
	return &banR{}
}

// banL is where Load methods for each relationship are stored.
type banL struct{}

var (
	banColumns               = []string{"nodeid"}
	banColumnsWithoutDefault = []string{"nodeid"}
	banColumnsWithDefault    = []string{}
	banPrimaryKeyColumns     = []string{"nodeid"}
)

type (
	// BanSlice is an alias for a slice of pointers to Ban.
	// This should generally be used opposed to []Ban.
	BanSlice []*Ban
	// BanHook is the signature for custom Ban hook methods
	BanHook func(boil.Executor, *Ban) error

	banQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	banType                 = reflect.TypeOf(&Ban{})
	banMapping              = queries.MakeStructMapping(banType)
	banPrimaryKeyMapping, _ = queries.BindMapping(banType, banMapping, banPrimaryKeyColumns)
	banInsertCacheMut       sync.RWMutex
	banInsertCache          = make(map[string]insertCache)
	banUpdateCacheMut       sync.RWMutex
	banUpdateCache          = make(map[string]updateCache)
	banUpsertCacheMut       sync.RWMutex
	banUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
)

var banBeforeInsertHooks []BanHook
var banBeforeUpdateHooks []BanHook
var banBeforeDeleteHooks []BanHook
var banBeforeUpsertHooks []BanHook

var banAfterInsertHooks []BanHook
var banAfterSelectHooks []BanHook
var banAfterUpdateHooks []BanHook
var banAfterDeleteHooks []BanHook
var banAfterUpsertHooks []BanHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *Ban) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range banBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *Ban) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range banBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *Ban) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range banBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *Ban) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range banBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *Ban) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range banAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *Ban) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range banAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *Ban) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range banAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *Ban) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range banAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *Ban) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range banAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddBanHook registers your hook function for all future operations.
func AddBanHook(hookPoint boil.HookPoint, banHook BanHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		banBeforeInsertHooks = append(banBeforeInsertHooks, banHook)
	case boil.BeforeUpdateHook:
		banBeforeUpdateHooks = append(banBeforeUpdateHooks, banHook)
	case boil.BeforeDeleteHook:
		banBeforeDeleteHooks = append(banBeforeDeleteHooks, banHook)
	case boil.BeforeUpsertHook:
		banBeforeUpsertHooks = append(banBeforeUpsertHooks, banHook)
	case boil.AfterInsertHook:
		banAfterInsertHooks = append(banAfterInsertHooks, banHook)
	case boil.AfterSelectHook:
		banAfterSelectHooks = append(banAfterSelectHooks, banHook)
	case boil.AfterUpdateHook:
		banAfterUpdateHooks = append(banAfterUpdateHooks, banHook)
	case boil.AfterDeleteHook:
		banAfterDeleteHooks = append(banAfterDeleteHooks, banHook)
	case boil.AfterUpsertHook:
		banAfterUpsertHooks = append(banAfterUpsertHooks, banHook)
	}
}

// OneG returns a single ban record from the query using the global executor.
func (q banQuery) OneG() (*Ban, error) {
	return q.One(boil.GetDB())
}

// One returns a single ban record from the query.
func (q banQuery) One(exec boil.Executor) (*Ban, error) {
	o := &Ban{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "stellarcore: failed to execute a one query for ban")
	}

	if err := o.doAfterSelectHooks(exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all Ban records from the query using the global executor.
func (q banQuery) AllG() (BanSlice, error) {
	return q.All(boil.GetDB())
}

// All returns all Ban records from the query.
func (q banQuery) All(exec boil.Executor) (BanSlice, error) {
	var o []*Ban

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "stellarcore: failed to assign all query results to Ban slice")
	}

	if len(banAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all Ban records in the query, and panics on error.
func (q banQuery) CountG() (int64, error) {
	return q.Count(boil.GetDB())
}

// Count returns the count of all Ban records in the query.
func (q banQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: failed to count ban rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table, and panics on error.
func (q banQuery) ExistsG() (bool, error) {
	return q.Exists(boil.GetDB())
}

// Exists checks if the row exists in the table.
func (q banQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "stellarcore: failed to check if ban exists")
	}

	return count > 0, nil
}

// Bans retrieves all the records using an executor.
func Bans(mods ...qm.QueryMod) banQuery {
	mods = append(mods, qm.From("\"ban\""))
	return banQuery{NewQuery(mods...)}
}

// FindBanG retrieves a single record by ID.
func FindBanG(nodeid string, selectCols ...string) (*Ban, error) {
	return FindBan(boil.GetDB(), nodeid, selectCols...)
}

// FindBan retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindBan(exec boil.Executor, nodeid string, selectCols ...string) (*Ban, error) {
	banObj := &Ban{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"ban\" where \"nodeid\"=$1", sel,
	)

	q := queries.Raw(query, nodeid)

	err := q.Bind(nil, exec, banObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "stellarcore: unable to select from ban")
	}

	return banObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *Ban) InsertG(columns boil.Columns) error {
	return o.Insert(boil.GetDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Ban) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("stellarcore: no ban provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(banColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	banInsertCacheMut.RLock()
	cache, cached := banInsertCache[key]
	banInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			banColumns,
			banColumnsWithDefault,
			banColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(banType, banMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(banType, banMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"ban\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"ban\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "stellarcore: unable to insert into ban")
	}

	if !cached {
		banInsertCacheMut.Lock()
		banInsertCache[key] = cache
		banInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single Ban record using the global executor.
// See Update for more documentation.
func (o *Ban) UpdateG(columns boil.Columns) (int64, error) {
	return o.Update(boil.GetDB(), columns)
}

// Update uses an executor to update the Ban.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Ban) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	banUpdateCacheMut.RLock()
	cache, cached := banUpdateCache[key]
	banUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			banColumns,
			banPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("stellarcore: unable to update ban, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"ban\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, banPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(banType, banMapping, append(wl, banPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to update ban row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: failed to get rows affected by update for ban")
	}

	if !cached {
		banUpdateCacheMut.Lock()
		banUpdateCache[key] = cache
		banUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(exec)
}

// UpdateAll updates all rows with the specified column values.
func (q banQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to update all for ban")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to retrieve rows affected for ban")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o BanSlice) UpdateAllG(cols M) (int64, error) {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o BanSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("stellarcore: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), banPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"ban\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, banPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to update all in ban slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to retrieve rows affected all in update all ban")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *Ban) UpsertG(updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	return o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Ban) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("stellarcore: no ban provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(banColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	banUpsertCacheMut.RLock()
	cache, cached := banUpsertCache[key]
	banUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			banColumns,
			banColumnsWithDefault,
			banColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			banColumns,
			banPrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("stellarcore: unable to upsert ban, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(banPrimaryKeyColumns))
			copy(conflict, banPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"ban\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(banType, banMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(banType, banMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "stellarcore: unable to upsert ban")
	}

	if !cached {
		banUpsertCacheMut.Lock()
		banUpsertCache[key] = cache
		banUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteG deletes a single Ban record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *Ban) DeleteG() (int64, error) {
	return o.Delete(boil.GetDB())
}

// Delete deletes a single Ban record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Ban) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("stellarcore: no Ban provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), banPrimaryKeyMapping)
	sql := "DELETE FROM \"ban\" WHERE \"nodeid\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to delete from ban")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: failed to get rows affected by delete for ban")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q banQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("stellarcore: no banQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to delete all from ban")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: failed to get rows affected by deleteall for ban")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o BanSlice) DeleteAllG() (int64, error) {
	return o.DeleteAll(boil.GetDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o BanSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("stellarcore: no Ban slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(banBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), banPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"ban\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, banPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to delete all from ban slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: failed to get rows affected by deleteall for ban")
	}

	if len(banAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *Ban) ReloadG() error {
	if o == nil {
		return errors.New("stellarcore: no Ban provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Ban) Reload(exec boil.Executor) error {
	ret, err := FindBan(exec, o.Nodeid)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *BanSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("stellarcore: empty BanSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *BanSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := BanSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), banPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"ban\".* FROM \"ban\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, banPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "stellarcore: unable to reload all in BanSlice")
	}

	*o = slice

	return nil
}

// BanExistsG checks if the Ban row exists.
func BanExistsG(nodeid string) (bool, error) {
	return BanExists(boil.GetDB(), nodeid)
}

// BanExists checks if the Ban row exists.
func BanExists(exec boil.Executor, nodeid string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"ban\" where \"nodeid\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, nodeid)
	}

	row := exec.QueryRow(sql, nodeid)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "stellarcore: unable to check if ban exists")
	}

	return exists, nil
}
