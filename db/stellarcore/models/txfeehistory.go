// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package stellarcore

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// Txfeehistory is an object representing the database table.
type Txfeehistory struct {
	Txid      string `boil:"txid" json:"txid" toml:"txid" yaml:"txid"`
	Ledgerseq int    `boil:"ledgerseq" json:"ledgerseq" toml:"ledgerseq" yaml:"ledgerseq"`
	Txindex   int    `boil:"txindex" json:"txindex" toml:"txindex" yaml:"txindex"`
	Txchanges string `boil:"txchanges" json:"txchanges" toml:"txchanges" yaml:"txchanges"`

	R *txfeehistoryR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L txfeehistoryL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TxfeehistoryColumns = struct {
	Txid      string
	Ledgerseq string
	Txindex   string
	Txchanges string
}{
	Txid:      "txid",
	Ledgerseq: "ledgerseq",
	Txindex:   "txindex",
	Txchanges: "txchanges",
}

// TxfeehistoryRels is where relationship names are stored.
var TxfeehistoryRels = struct {
}{}

// txfeehistoryR is where relationships are stored.
type txfeehistoryR struct {
}

// NewStruct creates a new relationship struct
func (*txfeehistoryR) NewStruct() *txfeehistoryR {
	return &txfeehistoryR{}
}

// txfeehistoryL is where Load methods for each relationship are stored.
type txfeehistoryL struct{}

var (
	txfeehistoryColumns               = []string{"txid", "ledgerseq", "txindex", "txchanges"}
	txfeehistoryColumnsWithoutDefault = []string{"txid", "ledgerseq", "txindex", "txchanges"}
	txfeehistoryColumnsWithDefault    = []string{}
	txfeehistoryPrimaryKeyColumns     = []string{"ledgerseq", "txindex"}
)

type (
	// TxfeehistorySlice is an alias for a slice of pointers to Txfeehistory.
	// This should generally be used opposed to []Txfeehistory.
	TxfeehistorySlice []*Txfeehistory
	// TxfeehistoryHook is the signature for custom Txfeehistory hook methods
	TxfeehistoryHook func(boil.Executor, *Txfeehistory) error

	txfeehistoryQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	txfeehistoryType                 = reflect.TypeOf(&Txfeehistory{})
	txfeehistoryMapping              = queries.MakeStructMapping(txfeehistoryType)
	txfeehistoryPrimaryKeyMapping, _ = queries.BindMapping(txfeehistoryType, txfeehistoryMapping, txfeehistoryPrimaryKeyColumns)
	txfeehistoryInsertCacheMut       sync.RWMutex
	txfeehistoryInsertCache          = make(map[string]insertCache)
	txfeehistoryUpdateCacheMut       sync.RWMutex
	txfeehistoryUpdateCache          = make(map[string]updateCache)
	txfeehistoryUpsertCacheMut       sync.RWMutex
	txfeehistoryUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
)

var txfeehistoryBeforeInsertHooks []TxfeehistoryHook
var txfeehistoryBeforeUpdateHooks []TxfeehistoryHook
var txfeehistoryBeforeDeleteHooks []TxfeehistoryHook
var txfeehistoryBeforeUpsertHooks []TxfeehistoryHook

var txfeehistoryAfterInsertHooks []TxfeehistoryHook
var txfeehistoryAfterSelectHooks []TxfeehistoryHook
var txfeehistoryAfterUpdateHooks []TxfeehistoryHook
var txfeehistoryAfterDeleteHooks []TxfeehistoryHook
var txfeehistoryAfterUpsertHooks []TxfeehistoryHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *Txfeehistory) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range txfeehistoryBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *Txfeehistory) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range txfeehistoryBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *Txfeehistory) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range txfeehistoryBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *Txfeehistory) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range txfeehistoryBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *Txfeehistory) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range txfeehistoryAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *Txfeehistory) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range txfeehistoryAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *Txfeehistory) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range txfeehistoryAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *Txfeehistory) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range txfeehistoryAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *Txfeehistory) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range txfeehistoryAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTxfeehistoryHook registers your hook function for all future operations.
func AddTxfeehistoryHook(hookPoint boil.HookPoint, txfeehistoryHook TxfeehistoryHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		txfeehistoryBeforeInsertHooks = append(txfeehistoryBeforeInsertHooks, txfeehistoryHook)
	case boil.BeforeUpdateHook:
		txfeehistoryBeforeUpdateHooks = append(txfeehistoryBeforeUpdateHooks, txfeehistoryHook)
	case boil.BeforeDeleteHook:
		txfeehistoryBeforeDeleteHooks = append(txfeehistoryBeforeDeleteHooks, txfeehistoryHook)
	case boil.BeforeUpsertHook:
		txfeehistoryBeforeUpsertHooks = append(txfeehistoryBeforeUpsertHooks, txfeehistoryHook)
	case boil.AfterInsertHook:
		txfeehistoryAfterInsertHooks = append(txfeehistoryAfterInsertHooks, txfeehistoryHook)
	case boil.AfterSelectHook:
		txfeehistoryAfterSelectHooks = append(txfeehistoryAfterSelectHooks, txfeehistoryHook)
	case boil.AfterUpdateHook:
		txfeehistoryAfterUpdateHooks = append(txfeehistoryAfterUpdateHooks, txfeehistoryHook)
	case boil.AfterDeleteHook:
		txfeehistoryAfterDeleteHooks = append(txfeehistoryAfterDeleteHooks, txfeehistoryHook)
	case boil.AfterUpsertHook:
		txfeehistoryAfterUpsertHooks = append(txfeehistoryAfterUpsertHooks, txfeehistoryHook)
	}
}

// OneG returns a single txfeehistory record from the query using the global executor.
func (q txfeehistoryQuery) OneG() (*Txfeehistory, error) {
	return q.One(boil.GetDB())
}

// One returns a single txfeehistory record from the query.
func (q txfeehistoryQuery) One(exec boil.Executor) (*Txfeehistory, error) {
	o := &Txfeehistory{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "stellarcore: failed to execute a one query for txfeehistory")
	}

	if err := o.doAfterSelectHooks(exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all Txfeehistory records from the query using the global executor.
func (q txfeehistoryQuery) AllG() (TxfeehistorySlice, error) {
	return q.All(boil.GetDB())
}

// All returns all Txfeehistory records from the query.
func (q txfeehistoryQuery) All(exec boil.Executor) (TxfeehistorySlice, error) {
	var o []*Txfeehistory

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "stellarcore: failed to assign all query results to Txfeehistory slice")
	}

	if len(txfeehistoryAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all Txfeehistory records in the query, and panics on error.
func (q txfeehistoryQuery) CountG() (int64, error) {
	return q.Count(boil.GetDB())
}

// Count returns the count of all Txfeehistory records in the query.
func (q txfeehistoryQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: failed to count txfeehistory rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table, and panics on error.
func (q txfeehistoryQuery) ExistsG() (bool, error) {
	return q.Exists(boil.GetDB())
}

// Exists checks if the row exists in the table.
func (q txfeehistoryQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "stellarcore: failed to check if txfeehistory exists")
	}

	return count > 0, nil
}

// Txfeehistories retrieves all the records using an executor.
func Txfeehistories(mods ...qm.QueryMod) txfeehistoryQuery {
	mods = append(mods, qm.From("\"txfeehistory\""))
	return txfeehistoryQuery{NewQuery(mods...)}
}

// FindTxfeehistoryG retrieves a single record by ID.
func FindTxfeehistoryG(ledgerseq int, txindex int, selectCols ...string) (*Txfeehistory, error) {
	return FindTxfeehistory(boil.GetDB(), ledgerseq, txindex, selectCols...)
}

// FindTxfeehistory retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTxfeehistory(exec boil.Executor, ledgerseq int, txindex int, selectCols ...string) (*Txfeehistory, error) {
	txfeehistoryObj := &Txfeehistory{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"txfeehistory\" where \"ledgerseq\"=$1 AND \"txindex\"=$2", sel,
	)

	q := queries.Raw(query, ledgerseq, txindex)

	err := q.Bind(nil, exec, txfeehistoryObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "stellarcore: unable to select from txfeehistory")
	}

	return txfeehistoryObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *Txfeehistory) InsertG(columns boil.Columns) error {
	return o.Insert(boil.GetDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Txfeehistory) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("stellarcore: no txfeehistory provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(txfeehistoryColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	txfeehistoryInsertCacheMut.RLock()
	cache, cached := txfeehistoryInsertCache[key]
	txfeehistoryInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			txfeehistoryColumns,
			txfeehistoryColumnsWithDefault,
			txfeehistoryColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(txfeehistoryType, txfeehistoryMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(txfeehistoryType, txfeehistoryMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"txfeehistory\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"txfeehistory\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "stellarcore: unable to insert into txfeehistory")
	}

	if !cached {
		txfeehistoryInsertCacheMut.Lock()
		txfeehistoryInsertCache[key] = cache
		txfeehistoryInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single Txfeehistory record using the global executor.
// See Update for more documentation.
func (o *Txfeehistory) UpdateG(columns boil.Columns) (int64, error) {
	return o.Update(boil.GetDB(), columns)
}

// Update uses an executor to update the Txfeehistory.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Txfeehistory) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	txfeehistoryUpdateCacheMut.RLock()
	cache, cached := txfeehistoryUpdateCache[key]
	txfeehistoryUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			txfeehistoryColumns,
			txfeehistoryPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("stellarcore: unable to update txfeehistory, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"txfeehistory\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, txfeehistoryPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(txfeehistoryType, txfeehistoryMapping, append(wl, txfeehistoryPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to update txfeehistory row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: failed to get rows affected by update for txfeehistory")
	}

	if !cached {
		txfeehistoryUpdateCacheMut.Lock()
		txfeehistoryUpdateCache[key] = cache
		txfeehistoryUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(exec)
}

// UpdateAll updates all rows with the specified column values.
func (q txfeehistoryQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to update all for txfeehistory")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to retrieve rows affected for txfeehistory")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o TxfeehistorySlice) UpdateAllG(cols M) (int64, error) {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TxfeehistorySlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("stellarcore: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), txfeehistoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"txfeehistory\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, txfeehistoryPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to update all in txfeehistory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to retrieve rows affected all in update all txfeehistory")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *Txfeehistory) UpsertG(updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	return o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Txfeehistory) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("stellarcore: no txfeehistory provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(txfeehistoryColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	txfeehistoryUpsertCacheMut.RLock()
	cache, cached := txfeehistoryUpsertCache[key]
	txfeehistoryUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			txfeehistoryColumns,
			txfeehistoryColumnsWithDefault,
			txfeehistoryColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			txfeehistoryColumns,
			txfeehistoryPrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("stellarcore: unable to upsert txfeehistory, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(txfeehistoryPrimaryKeyColumns))
			copy(conflict, txfeehistoryPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"txfeehistory\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(txfeehistoryType, txfeehistoryMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(txfeehistoryType, txfeehistoryMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "stellarcore: unable to upsert txfeehistory")
	}

	if !cached {
		txfeehistoryUpsertCacheMut.Lock()
		txfeehistoryUpsertCache[key] = cache
		txfeehistoryUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteG deletes a single Txfeehistory record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *Txfeehistory) DeleteG() (int64, error) {
	return o.Delete(boil.GetDB())
}

// Delete deletes a single Txfeehistory record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Txfeehistory) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("stellarcore: no Txfeehistory provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), txfeehistoryPrimaryKeyMapping)
	sql := "DELETE FROM \"txfeehistory\" WHERE \"ledgerseq\"=$1 AND \"txindex\"=$2"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to delete from txfeehistory")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: failed to get rows affected by delete for txfeehistory")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q txfeehistoryQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("stellarcore: no txfeehistoryQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to delete all from txfeehistory")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: failed to get rows affected by deleteall for txfeehistory")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o TxfeehistorySlice) DeleteAllG() (int64, error) {
	return o.DeleteAll(boil.GetDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TxfeehistorySlice) DeleteAll(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("stellarcore: no Txfeehistory slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(txfeehistoryBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), txfeehistoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"txfeehistory\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, txfeehistoryPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to delete all from txfeehistory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: failed to get rows affected by deleteall for txfeehistory")
	}

	if len(txfeehistoryAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *Txfeehistory) ReloadG() error {
	if o == nil {
		return errors.New("stellarcore: no Txfeehistory provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Txfeehistory) Reload(exec boil.Executor) error {
	ret, err := FindTxfeehistory(exec, o.Ledgerseq, o.Txindex)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TxfeehistorySlice) ReloadAllG() error {
	if o == nil {
		return errors.New("stellarcore: empty TxfeehistorySlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TxfeehistorySlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TxfeehistorySlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), txfeehistoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"txfeehistory\".* FROM \"txfeehistory\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, txfeehistoryPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "stellarcore: unable to reload all in TxfeehistorySlice")
	}

	*o = slice

	return nil
}

// TxfeehistoryExistsG checks if the Txfeehistory row exists.
func TxfeehistoryExistsG(ledgerseq int, txindex int) (bool, error) {
	return TxfeehistoryExists(boil.GetDB(), ledgerseq, txindex)
}

// TxfeehistoryExists checks if the Txfeehistory row exists.
func TxfeehistoryExists(exec boil.Executor, ledgerseq int, txindex int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"txfeehistory\" where \"ledgerseq\"=$1 AND \"txindex\"=$2 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, ledgerseq, txindex)
	}

	row := exec.QueryRow(sql, ledgerseq, txindex)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "stellarcore: unable to check if txfeehistory exists")
	}

	return exists, nil
}
