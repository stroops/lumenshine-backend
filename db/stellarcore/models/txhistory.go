// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package stellarcore

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// Txhistory is an object representing the database table.
type Txhistory struct {
	Txid      string `boil:"txid" json:"txid" toml:"txid" yaml:"txid"`
	Ledgerseq int    `boil:"ledgerseq" json:"ledgerseq" toml:"ledgerseq" yaml:"ledgerseq"`
	Txindex   int    `boil:"txindex" json:"txindex" toml:"txindex" yaml:"txindex"`
	Txbody    string `boil:"txbody" json:"txbody" toml:"txbody" yaml:"txbody"`
	Txresult  string `boil:"txresult" json:"txresult" toml:"txresult" yaml:"txresult"`
	Txmeta    string `boil:"txmeta" json:"txmeta" toml:"txmeta" yaml:"txmeta"`

	R *txhistoryR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L txhistoryL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TxhistoryColumns = struct {
	Txid      string
	Ledgerseq string
	Txindex   string
	Txbody    string
	Txresult  string
	Txmeta    string
}{
	Txid:      "txid",
	Ledgerseq: "ledgerseq",
	Txindex:   "txindex",
	Txbody:    "txbody",
	Txresult:  "txresult",
	Txmeta:    "txmeta",
}

// TxhistoryRels is where relationship names are stored.
var TxhistoryRels = struct {
}{}

// txhistoryR is where relationships are stored.
type txhistoryR struct {
}

// NewStruct creates a new relationship struct
func (*txhistoryR) NewStruct() *txhistoryR {
	return &txhistoryR{}
}

// txhistoryL is where Load methods for each relationship are stored.
type txhistoryL struct{}

var (
	txhistoryColumns               = []string{"txid", "ledgerseq", "txindex", "txbody", "txresult", "txmeta"}
	txhistoryColumnsWithoutDefault = []string{"txid", "ledgerseq", "txindex", "txbody", "txresult", "txmeta"}
	txhistoryColumnsWithDefault    = []string{}
	txhistoryPrimaryKeyColumns     = []string{"ledgerseq", "txindex"}
)

type (
	// TxhistorySlice is an alias for a slice of pointers to Txhistory.
	// This should generally be used opposed to []Txhistory.
	TxhistorySlice []*Txhistory
	// TxhistoryHook is the signature for custom Txhistory hook methods
	TxhistoryHook func(boil.Executor, *Txhistory) error

	txhistoryQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	txhistoryType                 = reflect.TypeOf(&Txhistory{})
	txhistoryMapping              = queries.MakeStructMapping(txhistoryType)
	txhistoryPrimaryKeyMapping, _ = queries.BindMapping(txhistoryType, txhistoryMapping, txhistoryPrimaryKeyColumns)
	txhistoryInsertCacheMut       sync.RWMutex
	txhistoryInsertCache          = make(map[string]insertCache)
	txhistoryUpdateCacheMut       sync.RWMutex
	txhistoryUpdateCache          = make(map[string]updateCache)
	txhistoryUpsertCacheMut       sync.RWMutex
	txhistoryUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
)

var txhistoryBeforeInsertHooks []TxhistoryHook
var txhistoryBeforeUpdateHooks []TxhistoryHook
var txhistoryBeforeDeleteHooks []TxhistoryHook
var txhistoryBeforeUpsertHooks []TxhistoryHook

var txhistoryAfterInsertHooks []TxhistoryHook
var txhistoryAfterSelectHooks []TxhistoryHook
var txhistoryAfterUpdateHooks []TxhistoryHook
var txhistoryAfterDeleteHooks []TxhistoryHook
var txhistoryAfterUpsertHooks []TxhistoryHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *Txhistory) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range txhistoryBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *Txhistory) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range txhistoryBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *Txhistory) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range txhistoryBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *Txhistory) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range txhistoryBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *Txhistory) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range txhistoryAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *Txhistory) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range txhistoryAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *Txhistory) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range txhistoryAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *Txhistory) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range txhistoryAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *Txhistory) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range txhistoryAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTxhistoryHook registers your hook function for all future operations.
func AddTxhistoryHook(hookPoint boil.HookPoint, txhistoryHook TxhistoryHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		txhistoryBeforeInsertHooks = append(txhistoryBeforeInsertHooks, txhistoryHook)
	case boil.BeforeUpdateHook:
		txhistoryBeforeUpdateHooks = append(txhistoryBeforeUpdateHooks, txhistoryHook)
	case boil.BeforeDeleteHook:
		txhistoryBeforeDeleteHooks = append(txhistoryBeforeDeleteHooks, txhistoryHook)
	case boil.BeforeUpsertHook:
		txhistoryBeforeUpsertHooks = append(txhistoryBeforeUpsertHooks, txhistoryHook)
	case boil.AfterInsertHook:
		txhistoryAfterInsertHooks = append(txhistoryAfterInsertHooks, txhistoryHook)
	case boil.AfterSelectHook:
		txhistoryAfterSelectHooks = append(txhistoryAfterSelectHooks, txhistoryHook)
	case boil.AfterUpdateHook:
		txhistoryAfterUpdateHooks = append(txhistoryAfterUpdateHooks, txhistoryHook)
	case boil.AfterDeleteHook:
		txhistoryAfterDeleteHooks = append(txhistoryAfterDeleteHooks, txhistoryHook)
	case boil.AfterUpsertHook:
		txhistoryAfterUpsertHooks = append(txhistoryAfterUpsertHooks, txhistoryHook)
	}
}

// OneG returns a single txhistory record from the query using the global executor.
func (q txhistoryQuery) OneG() (*Txhistory, error) {
	return q.One(boil.GetDB())
}

// One returns a single txhistory record from the query.
func (q txhistoryQuery) One(exec boil.Executor) (*Txhistory, error) {
	o := &Txhistory{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "stellarcore: failed to execute a one query for txhistory")
	}

	if err := o.doAfterSelectHooks(exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all Txhistory records from the query using the global executor.
func (q txhistoryQuery) AllG() (TxhistorySlice, error) {
	return q.All(boil.GetDB())
}

// All returns all Txhistory records from the query.
func (q txhistoryQuery) All(exec boil.Executor) (TxhistorySlice, error) {
	var o []*Txhistory

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "stellarcore: failed to assign all query results to Txhistory slice")
	}

	if len(txhistoryAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all Txhistory records in the query, and panics on error.
func (q txhistoryQuery) CountG() (int64, error) {
	return q.Count(boil.GetDB())
}

// Count returns the count of all Txhistory records in the query.
func (q txhistoryQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: failed to count txhistory rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table, and panics on error.
func (q txhistoryQuery) ExistsG() (bool, error) {
	return q.Exists(boil.GetDB())
}

// Exists checks if the row exists in the table.
func (q txhistoryQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "stellarcore: failed to check if txhistory exists")
	}

	return count > 0, nil
}

// Txhistories retrieves all the records using an executor.
func Txhistories(mods ...qm.QueryMod) txhistoryQuery {
	mods = append(mods, qm.From("\"txhistory\""))
	return txhistoryQuery{NewQuery(mods...)}
}

// FindTxhistoryG retrieves a single record by ID.
func FindTxhistoryG(ledgerseq int, txindex int, selectCols ...string) (*Txhistory, error) {
	return FindTxhistory(boil.GetDB(), ledgerseq, txindex, selectCols...)
}

// FindTxhistory retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTxhistory(exec boil.Executor, ledgerseq int, txindex int, selectCols ...string) (*Txhistory, error) {
	txhistoryObj := &Txhistory{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"txhistory\" where \"ledgerseq\"=$1 AND \"txindex\"=$2", sel,
	)

	q := queries.Raw(query, ledgerseq, txindex)

	err := q.Bind(nil, exec, txhistoryObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "stellarcore: unable to select from txhistory")
	}

	return txhistoryObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *Txhistory) InsertG(columns boil.Columns) error {
	return o.Insert(boil.GetDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Txhistory) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("stellarcore: no txhistory provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(txhistoryColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	txhistoryInsertCacheMut.RLock()
	cache, cached := txhistoryInsertCache[key]
	txhistoryInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			txhistoryColumns,
			txhistoryColumnsWithDefault,
			txhistoryColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(txhistoryType, txhistoryMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(txhistoryType, txhistoryMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"txhistory\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"txhistory\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "stellarcore: unable to insert into txhistory")
	}

	if !cached {
		txhistoryInsertCacheMut.Lock()
		txhistoryInsertCache[key] = cache
		txhistoryInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single Txhistory record using the global executor.
// See Update for more documentation.
func (o *Txhistory) UpdateG(columns boil.Columns) (int64, error) {
	return o.Update(boil.GetDB(), columns)
}

// Update uses an executor to update the Txhistory.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Txhistory) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	txhistoryUpdateCacheMut.RLock()
	cache, cached := txhistoryUpdateCache[key]
	txhistoryUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			txhistoryColumns,
			txhistoryPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("stellarcore: unable to update txhistory, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"txhistory\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, txhistoryPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(txhistoryType, txhistoryMapping, append(wl, txhistoryPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to update txhistory row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: failed to get rows affected by update for txhistory")
	}

	if !cached {
		txhistoryUpdateCacheMut.Lock()
		txhistoryUpdateCache[key] = cache
		txhistoryUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(exec)
}

// UpdateAll updates all rows with the specified column values.
func (q txhistoryQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to update all for txhistory")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to retrieve rows affected for txhistory")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o TxhistorySlice) UpdateAllG(cols M) (int64, error) {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TxhistorySlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("stellarcore: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), txhistoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"txhistory\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, txhistoryPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to update all in txhistory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to retrieve rows affected all in update all txhistory")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *Txhistory) UpsertG(updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	return o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Txhistory) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("stellarcore: no txhistory provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(txhistoryColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	txhistoryUpsertCacheMut.RLock()
	cache, cached := txhistoryUpsertCache[key]
	txhistoryUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			txhistoryColumns,
			txhistoryColumnsWithDefault,
			txhistoryColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			txhistoryColumns,
			txhistoryPrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("stellarcore: unable to upsert txhistory, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(txhistoryPrimaryKeyColumns))
			copy(conflict, txhistoryPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"txhistory\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(txhistoryType, txhistoryMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(txhistoryType, txhistoryMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "stellarcore: unable to upsert txhistory")
	}

	if !cached {
		txhistoryUpsertCacheMut.Lock()
		txhistoryUpsertCache[key] = cache
		txhistoryUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteG deletes a single Txhistory record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *Txhistory) DeleteG() (int64, error) {
	return o.Delete(boil.GetDB())
}

// Delete deletes a single Txhistory record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Txhistory) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("stellarcore: no Txhistory provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), txhistoryPrimaryKeyMapping)
	sql := "DELETE FROM \"txhistory\" WHERE \"ledgerseq\"=$1 AND \"txindex\"=$2"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to delete from txhistory")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: failed to get rows affected by delete for txhistory")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q txhistoryQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("stellarcore: no txhistoryQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to delete all from txhistory")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: failed to get rows affected by deleteall for txhistory")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o TxhistorySlice) DeleteAllG() (int64, error) {
	return o.DeleteAll(boil.GetDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TxhistorySlice) DeleteAll(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("stellarcore: no Txhistory slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(txhistoryBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), txhistoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"txhistory\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, txhistoryPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: unable to delete all from txhistory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "stellarcore: failed to get rows affected by deleteall for txhistory")
	}

	if len(txhistoryAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *Txhistory) ReloadG() error {
	if o == nil {
		return errors.New("stellarcore: no Txhistory provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Txhistory) Reload(exec boil.Executor) error {
	ret, err := FindTxhistory(exec, o.Ledgerseq, o.Txindex)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TxhistorySlice) ReloadAllG() error {
	if o == nil {
		return errors.New("stellarcore: empty TxhistorySlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TxhistorySlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TxhistorySlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), txhistoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"txhistory\".* FROM \"txhistory\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, txhistoryPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "stellarcore: unable to reload all in TxhistorySlice")
	}

	*o = slice

	return nil
}

// TxhistoryExistsG checks if the Txhistory row exists.
func TxhistoryExistsG(ledgerseq int, txindex int) (bool, error) {
	return TxhistoryExists(boil.GetDB(), ledgerseq, txindex)
}

// TxhistoryExists checks if the Txhistory row exists.
func TxhistoryExists(exec boil.Executor, ledgerseq int, txindex int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"txhistory\" where \"ledgerseq\"=$1 AND \"txindex\"=$2 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, ledgerseq, txindex)
	}

	row := exec.QueryRow(sql, ledgerseq, txindex)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "stellarcore: unable to check if txhistory exists")
	}

	return exists, nil
}
