// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package horizon

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// SseIndex is an object representing the database table.
type SseIndex struct {
	ID    int    `boil:"id" json:"id" toml:"id" yaml:"id"`
	Name  string `boil:"name" json:"name" toml:"name" yaml:"name"`
	Value int64  `boil:"value" json:"value" toml:"value" yaml:"value"`

	R *sseIndexR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L sseIndexL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var SseIndexColumns = struct {
	ID    string
	Name  string
	Value string
}{
	ID:    "id",
	Name:  "name",
	Value: "value",
}

// SseIndexRels is where relationship names are stored.
var SseIndexRels = struct {
}{}

// sseIndexR is where relationships are stored.
type sseIndexR struct {
}

// NewStruct creates a new relationship struct
func (*sseIndexR) NewStruct() *sseIndexR {
	return &sseIndexR{}
}

// sseIndexL is where Load methods for each relationship are stored.
type sseIndexL struct{}

var (
	sseIndexColumns               = []string{"id", "name", "value"}
	sseIndexColumnsWithoutDefault = []string{"name"}
	sseIndexColumnsWithDefault    = []string{"id", "value"}
	sseIndexPrimaryKeyColumns     = []string{"id"}
)

type (
	// SseIndexSlice is an alias for a slice of pointers to SseIndex.
	// This should generally be used opposed to []SseIndex.
	SseIndexSlice []*SseIndex
	// SseIndexHook is the signature for custom SseIndex hook methods
	SseIndexHook func(boil.Executor, *SseIndex) error

	sseIndexQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	sseIndexType                 = reflect.TypeOf(&SseIndex{})
	sseIndexMapping              = queries.MakeStructMapping(sseIndexType)
	sseIndexPrimaryKeyMapping, _ = queries.BindMapping(sseIndexType, sseIndexMapping, sseIndexPrimaryKeyColumns)
	sseIndexInsertCacheMut       sync.RWMutex
	sseIndexInsertCache          = make(map[string]insertCache)
	sseIndexUpdateCacheMut       sync.RWMutex
	sseIndexUpdateCache          = make(map[string]updateCache)
	sseIndexUpsertCacheMut       sync.RWMutex
	sseIndexUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
)

var sseIndexBeforeInsertHooks []SseIndexHook
var sseIndexBeforeUpdateHooks []SseIndexHook
var sseIndexBeforeDeleteHooks []SseIndexHook
var sseIndexBeforeUpsertHooks []SseIndexHook

var sseIndexAfterInsertHooks []SseIndexHook
var sseIndexAfterSelectHooks []SseIndexHook
var sseIndexAfterUpdateHooks []SseIndexHook
var sseIndexAfterDeleteHooks []SseIndexHook
var sseIndexAfterUpsertHooks []SseIndexHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *SseIndex) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range sseIndexBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *SseIndex) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range sseIndexBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *SseIndex) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range sseIndexBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *SseIndex) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range sseIndexBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *SseIndex) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range sseIndexAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *SseIndex) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range sseIndexAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *SseIndex) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range sseIndexAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *SseIndex) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range sseIndexAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *SseIndex) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range sseIndexAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddSseIndexHook registers your hook function for all future operations.
func AddSseIndexHook(hookPoint boil.HookPoint, sseIndexHook SseIndexHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		sseIndexBeforeInsertHooks = append(sseIndexBeforeInsertHooks, sseIndexHook)
	case boil.BeforeUpdateHook:
		sseIndexBeforeUpdateHooks = append(sseIndexBeforeUpdateHooks, sseIndexHook)
	case boil.BeforeDeleteHook:
		sseIndexBeforeDeleteHooks = append(sseIndexBeforeDeleteHooks, sseIndexHook)
	case boil.BeforeUpsertHook:
		sseIndexBeforeUpsertHooks = append(sseIndexBeforeUpsertHooks, sseIndexHook)
	case boil.AfterInsertHook:
		sseIndexAfterInsertHooks = append(sseIndexAfterInsertHooks, sseIndexHook)
	case boil.AfterSelectHook:
		sseIndexAfterSelectHooks = append(sseIndexAfterSelectHooks, sseIndexHook)
	case boil.AfterUpdateHook:
		sseIndexAfterUpdateHooks = append(sseIndexAfterUpdateHooks, sseIndexHook)
	case boil.AfterDeleteHook:
		sseIndexAfterDeleteHooks = append(sseIndexAfterDeleteHooks, sseIndexHook)
	case boil.AfterUpsertHook:
		sseIndexAfterUpsertHooks = append(sseIndexAfterUpsertHooks, sseIndexHook)
	}
}

// OneG returns a single sseIndex record from the query using the global executor.
func (q sseIndexQuery) OneG() (*SseIndex, error) {
	return q.One(boil.GetDB())
}

// One returns a single sseIndex record from the query.
func (q sseIndexQuery) One(exec boil.Executor) (*SseIndex, error) {
	o := &SseIndex{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "horizon: failed to execute a one query for sse_index")
	}

	if err := o.doAfterSelectHooks(exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all SseIndex records from the query using the global executor.
func (q sseIndexQuery) AllG() (SseIndexSlice, error) {
	return q.All(boil.GetDB())
}

// All returns all SseIndex records from the query.
func (q sseIndexQuery) All(exec boil.Executor) (SseIndexSlice, error) {
	var o []*SseIndex

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "horizon: failed to assign all query results to SseIndex slice")
	}

	if len(sseIndexAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all SseIndex records in the query, and panics on error.
func (q sseIndexQuery) CountG() (int64, error) {
	return q.Count(boil.GetDB())
}

// Count returns the count of all SseIndex records in the query.
func (q sseIndexQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "horizon: failed to count sse_index rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table, and panics on error.
func (q sseIndexQuery) ExistsG() (bool, error) {
	return q.Exists(boil.GetDB())
}

// Exists checks if the row exists in the table.
func (q sseIndexQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "horizon: failed to check if sse_index exists")
	}

	return count > 0, nil
}

// SseIndices retrieves all the records using an executor.
func SseIndices(mods ...qm.QueryMod) sseIndexQuery {
	mods = append(mods, qm.From("\"sse_index\""))
	return sseIndexQuery{NewQuery(mods...)}
}

// FindSseIndexG retrieves a single record by ID.
func FindSseIndexG(iD int, selectCols ...string) (*SseIndex, error) {
	return FindSseIndex(boil.GetDB(), iD, selectCols...)
}

// FindSseIndex retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSseIndex(exec boil.Executor, iD int, selectCols ...string) (*SseIndex, error) {
	sseIndexObj := &SseIndex{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"sse_index\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, sseIndexObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "horizon: unable to select from sse_index")
	}

	return sseIndexObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *SseIndex) InsertG(columns boil.Columns) error {
	return o.Insert(boil.GetDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *SseIndex) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("horizon: no sse_index provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(sseIndexColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	sseIndexInsertCacheMut.RLock()
	cache, cached := sseIndexInsertCache[key]
	sseIndexInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			sseIndexColumns,
			sseIndexColumnsWithDefault,
			sseIndexColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(sseIndexType, sseIndexMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(sseIndexType, sseIndexMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"sse_index\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"sse_index\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "horizon: unable to insert into sse_index")
	}

	if !cached {
		sseIndexInsertCacheMut.Lock()
		sseIndexInsertCache[key] = cache
		sseIndexInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single SseIndex record using the global executor.
// See Update for more documentation.
func (o *SseIndex) UpdateG(columns boil.Columns) (int64, error) {
	return o.Update(boil.GetDB(), columns)
}

// Update uses an executor to update the SseIndex.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *SseIndex) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	sseIndexUpdateCacheMut.RLock()
	cache, cached := sseIndexUpdateCache[key]
	sseIndexUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			sseIndexColumns,
			sseIndexPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("horizon: unable to update sse_index, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"sse_index\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, sseIndexPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(sseIndexType, sseIndexMapping, append(wl, sseIndexPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "horizon: unable to update sse_index row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "horizon: failed to get rows affected by update for sse_index")
	}

	if !cached {
		sseIndexUpdateCacheMut.Lock()
		sseIndexUpdateCache[key] = cache
		sseIndexUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(exec)
}

// UpdateAll updates all rows with the specified column values.
func (q sseIndexQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "horizon: unable to update all for sse_index")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "horizon: unable to retrieve rows affected for sse_index")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o SseIndexSlice) UpdateAllG(cols M) (int64, error) {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SseIndexSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("horizon: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), sseIndexPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"sse_index\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, sseIndexPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "horizon: unable to update all in sseIndex slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "horizon: unable to retrieve rows affected all in update all sseIndex")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *SseIndex) UpsertG(updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	return o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *SseIndex) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("horizon: no sse_index provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(sseIndexColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	sseIndexUpsertCacheMut.RLock()
	cache, cached := sseIndexUpsertCache[key]
	sseIndexUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			sseIndexColumns,
			sseIndexColumnsWithDefault,
			sseIndexColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			sseIndexColumns,
			sseIndexPrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("horizon: unable to upsert sse_index, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(sseIndexPrimaryKeyColumns))
			copy(conflict, sseIndexPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"sse_index\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(sseIndexType, sseIndexMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(sseIndexType, sseIndexMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "horizon: unable to upsert sse_index")
	}

	if !cached {
		sseIndexUpsertCacheMut.Lock()
		sseIndexUpsertCache[key] = cache
		sseIndexUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteG deletes a single SseIndex record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *SseIndex) DeleteG() (int64, error) {
	return o.Delete(boil.GetDB())
}

// Delete deletes a single SseIndex record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *SseIndex) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("horizon: no SseIndex provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), sseIndexPrimaryKeyMapping)
	sql := "DELETE FROM \"sse_index\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "horizon: unable to delete from sse_index")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "horizon: failed to get rows affected by delete for sse_index")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q sseIndexQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("horizon: no sseIndexQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "horizon: unable to delete all from sse_index")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "horizon: failed to get rows affected by deleteall for sse_index")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o SseIndexSlice) DeleteAllG() (int64, error) {
	return o.DeleteAll(boil.GetDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SseIndexSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("horizon: no SseIndex slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(sseIndexBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), sseIndexPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"sse_index\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, sseIndexPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "horizon: unable to delete all from sseIndex slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "horizon: failed to get rows affected by deleteall for sse_index")
	}

	if len(sseIndexAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *SseIndex) ReloadG() error {
	if o == nil {
		return errors.New("horizon: no SseIndex provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *SseIndex) Reload(exec boil.Executor) error {
	ret, err := FindSseIndex(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SseIndexSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("horizon: empty SseIndexSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SseIndexSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := SseIndexSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), sseIndexPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"sse_index\".* FROM \"sse_index\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, sseIndexPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "horizon: unable to reload all in SseIndexSlice")
	}

	*o = slice

	return nil
}

// SseIndexExistsG checks if the SseIndex row exists.
func SseIndexExistsG(iD int) (bool, error) {
	return SseIndexExists(boil.GetDB(), iD)
}

// SseIndexExists checks if the SseIndex row exists.
func SseIndexExists(exec boil.Executor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"sse_index\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}

	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "horizon: unable to check if sse_index exists")
	}

	return exists, nil
}
