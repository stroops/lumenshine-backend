// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package horizon

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// HistoryEffect is an object representing the database table.
type HistoryEffect struct {
	HistoryAccountID   int64     `boil:"history_account_id" json:"history_account_id" toml:"history_account_id" yaml:"history_account_id"`
	HistoryOperationID int64     `boil:"history_operation_id" json:"history_operation_id" toml:"history_operation_id" yaml:"history_operation_id"`
	Order              int       `boil:"order" json:"order" toml:"order" yaml:"order"`
	Type               int       `boil:"type" json:"type" toml:"type" yaml:"type"`
	Details            null.JSON `boil:"details" json:"details,omitempty" toml:"details" yaml:"details,omitempty"`

	R *historyEffectR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L historyEffectL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var HistoryEffectColumns = struct {
	HistoryAccountID   string
	HistoryOperationID string
	Order              string
	Type               string
	Details            string
}{
	HistoryAccountID:   "history_account_id",
	HistoryOperationID: "history_operation_id",
	Order:              "order",
	Type:               "type",
	Details:            "details",
}

// HistoryEffectRels is where relationship names are stored.
var HistoryEffectRels = struct {
}{}

// historyEffectR is where relationships are stored.
type historyEffectR struct {
}

// NewStruct creates a new relationship struct
func (*historyEffectR) NewStruct() *historyEffectR {
	return &historyEffectR{}
}

// historyEffectL is where Load methods for each relationship are stored.
type historyEffectL struct{}

var (
	historyEffectColumns               = []string{"history_account_id", "history_operation_id", "order", "type", "details"}
	historyEffectColumnsWithoutDefault = []string{"history_account_id", "history_operation_id", "order", "type", "details"}
	historyEffectColumnsWithDefault    = []string{}
	historyEffectPrimaryKeyColumns     = []string{"history_account_id", "history_operation_id", "order"}
)

type (
	// HistoryEffectSlice is an alias for a slice of pointers to HistoryEffect.
	// This should generally be used opposed to []HistoryEffect.
	HistoryEffectSlice []*HistoryEffect
	// HistoryEffectHook is the signature for custom HistoryEffect hook methods
	HistoryEffectHook func(boil.Executor, *HistoryEffect) error

	historyEffectQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	historyEffectType                 = reflect.TypeOf(&HistoryEffect{})
	historyEffectMapping              = queries.MakeStructMapping(historyEffectType)
	historyEffectPrimaryKeyMapping, _ = queries.BindMapping(historyEffectType, historyEffectMapping, historyEffectPrimaryKeyColumns)
	historyEffectInsertCacheMut       sync.RWMutex
	historyEffectInsertCache          = make(map[string]insertCache)
	historyEffectUpdateCacheMut       sync.RWMutex
	historyEffectUpdateCache          = make(map[string]updateCache)
	historyEffectUpsertCacheMut       sync.RWMutex
	historyEffectUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
)

var historyEffectBeforeInsertHooks []HistoryEffectHook
var historyEffectBeforeUpdateHooks []HistoryEffectHook
var historyEffectBeforeDeleteHooks []HistoryEffectHook
var historyEffectBeforeUpsertHooks []HistoryEffectHook

var historyEffectAfterInsertHooks []HistoryEffectHook
var historyEffectAfterSelectHooks []HistoryEffectHook
var historyEffectAfterUpdateHooks []HistoryEffectHook
var historyEffectAfterDeleteHooks []HistoryEffectHook
var historyEffectAfterUpsertHooks []HistoryEffectHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *HistoryEffect) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range historyEffectBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *HistoryEffect) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range historyEffectBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *HistoryEffect) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range historyEffectBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *HistoryEffect) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range historyEffectBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *HistoryEffect) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range historyEffectAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *HistoryEffect) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range historyEffectAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *HistoryEffect) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range historyEffectAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *HistoryEffect) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range historyEffectAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *HistoryEffect) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range historyEffectAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddHistoryEffectHook registers your hook function for all future operations.
func AddHistoryEffectHook(hookPoint boil.HookPoint, historyEffectHook HistoryEffectHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		historyEffectBeforeInsertHooks = append(historyEffectBeforeInsertHooks, historyEffectHook)
	case boil.BeforeUpdateHook:
		historyEffectBeforeUpdateHooks = append(historyEffectBeforeUpdateHooks, historyEffectHook)
	case boil.BeforeDeleteHook:
		historyEffectBeforeDeleteHooks = append(historyEffectBeforeDeleteHooks, historyEffectHook)
	case boil.BeforeUpsertHook:
		historyEffectBeforeUpsertHooks = append(historyEffectBeforeUpsertHooks, historyEffectHook)
	case boil.AfterInsertHook:
		historyEffectAfterInsertHooks = append(historyEffectAfterInsertHooks, historyEffectHook)
	case boil.AfterSelectHook:
		historyEffectAfterSelectHooks = append(historyEffectAfterSelectHooks, historyEffectHook)
	case boil.AfterUpdateHook:
		historyEffectAfterUpdateHooks = append(historyEffectAfterUpdateHooks, historyEffectHook)
	case boil.AfterDeleteHook:
		historyEffectAfterDeleteHooks = append(historyEffectAfterDeleteHooks, historyEffectHook)
	case boil.AfterUpsertHook:
		historyEffectAfterUpsertHooks = append(historyEffectAfterUpsertHooks, historyEffectHook)
	}
}

// OneG returns a single historyEffect record from the query using the global executor.
func (q historyEffectQuery) OneG() (*HistoryEffect, error) {
	return q.One(boil.GetDB())
}

// One returns a single historyEffect record from the query.
func (q historyEffectQuery) One(exec boil.Executor) (*HistoryEffect, error) {
	o := &HistoryEffect{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "horizon: failed to execute a one query for history_effects")
	}

	if err := o.doAfterSelectHooks(exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all HistoryEffect records from the query using the global executor.
func (q historyEffectQuery) AllG() (HistoryEffectSlice, error) {
	return q.All(boil.GetDB())
}

// All returns all HistoryEffect records from the query.
func (q historyEffectQuery) All(exec boil.Executor) (HistoryEffectSlice, error) {
	var o []*HistoryEffect

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "horizon: failed to assign all query results to HistoryEffect slice")
	}

	if len(historyEffectAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all HistoryEffect records in the query, and panics on error.
func (q historyEffectQuery) CountG() (int64, error) {
	return q.Count(boil.GetDB())
}

// Count returns the count of all HistoryEffect records in the query.
func (q historyEffectQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "horizon: failed to count history_effects rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table, and panics on error.
func (q historyEffectQuery) ExistsG() (bool, error) {
	return q.Exists(boil.GetDB())
}

// Exists checks if the row exists in the table.
func (q historyEffectQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "horizon: failed to check if history_effects exists")
	}

	return count > 0, nil
}

// HistoryEffects retrieves all the records using an executor.
func HistoryEffects(mods ...qm.QueryMod) historyEffectQuery {
	mods = append(mods, qm.From("\"history_effects\""))
	return historyEffectQuery{NewQuery(mods...)}
}

// FindHistoryEffectG retrieves a single record by ID.
func FindHistoryEffectG(historyAccountID int64, historyOperationID int64, order int, selectCols ...string) (*HistoryEffect, error) {
	return FindHistoryEffect(boil.GetDB(), historyAccountID, historyOperationID, order, selectCols...)
}

// FindHistoryEffect retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindHistoryEffect(exec boil.Executor, historyAccountID int64, historyOperationID int64, order int, selectCols ...string) (*HistoryEffect, error) {
	historyEffectObj := &HistoryEffect{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"history_effects\" where \"history_account_id\"=$1 AND \"history_operation_id\"=$2 AND \"order\"=$3", sel,
	)

	q := queries.Raw(query, historyAccountID, historyOperationID, order)

	err := q.Bind(nil, exec, historyEffectObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "horizon: unable to select from history_effects")
	}

	return historyEffectObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *HistoryEffect) InsertG(columns boil.Columns) error {
	return o.Insert(boil.GetDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *HistoryEffect) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("horizon: no history_effects provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(historyEffectColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	historyEffectInsertCacheMut.RLock()
	cache, cached := historyEffectInsertCache[key]
	historyEffectInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			historyEffectColumns,
			historyEffectColumnsWithDefault,
			historyEffectColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(historyEffectType, historyEffectMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(historyEffectType, historyEffectMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"history_effects\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"history_effects\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "horizon: unable to insert into history_effects")
	}

	if !cached {
		historyEffectInsertCacheMut.Lock()
		historyEffectInsertCache[key] = cache
		historyEffectInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single HistoryEffect record using the global executor.
// See Update for more documentation.
func (o *HistoryEffect) UpdateG(columns boil.Columns) (int64, error) {
	return o.Update(boil.GetDB(), columns)
}

// Update uses an executor to update the HistoryEffect.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *HistoryEffect) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	historyEffectUpdateCacheMut.RLock()
	cache, cached := historyEffectUpdateCache[key]
	historyEffectUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			historyEffectColumns,
			historyEffectPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("horizon: unable to update history_effects, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"history_effects\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, historyEffectPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(historyEffectType, historyEffectMapping, append(wl, historyEffectPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "horizon: unable to update history_effects row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "horizon: failed to get rows affected by update for history_effects")
	}

	if !cached {
		historyEffectUpdateCacheMut.Lock()
		historyEffectUpdateCache[key] = cache
		historyEffectUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(exec)
}

// UpdateAll updates all rows with the specified column values.
func (q historyEffectQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "horizon: unable to update all for history_effects")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "horizon: unable to retrieve rows affected for history_effects")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o HistoryEffectSlice) UpdateAllG(cols M) (int64, error) {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o HistoryEffectSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("horizon: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), historyEffectPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"history_effects\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, historyEffectPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "horizon: unable to update all in historyEffect slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "horizon: unable to retrieve rows affected all in update all historyEffect")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *HistoryEffect) UpsertG(updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	return o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *HistoryEffect) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("horizon: no history_effects provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(historyEffectColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	historyEffectUpsertCacheMut.RLock()
	cache, cached := historyEffectUpsertCache[key]
	historyEffectUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			historyEffectColumns,
			historyEffectColumnsWithDefault,
			historyEffectColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			historyEffectColumns,
			historyEffectPrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("horizon: unable to upsert history_effects, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(historyEffectPrimaryKeyColumns))
			copy(conflict, historyEffectPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"history_effects\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(historyEffectType, historyEffectMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(historyEffectType, historyEffectMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "horizon: unable to upsert history_effects")
	}

	if !cached {
		historyEffectUpsertCacheMut.Lock()
		historyEffectUpsertCache[key] = cache
		historyEffectUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteG deletes a single HistoryEffect record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *HistoryEffect) DeleteG() (int64, error) {
	return o.Delete(boil.GetDB())
}

// Delete deletes a single HistoryEffect record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *HistoryEffect) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("horizon: no HistoryEffect provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), historyEffectPrimaryKeyMapping)
	sql := "DELETE FROM \"history_effects\" WHERE \"history_account_id\"=$1 AND \"history_operation_id\"=$2 AND \"order\"=$3"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "horizon: unable to delete from history_effects")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "horizon: failed to get rows affected by delete for history_effects")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q historyEffectQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("horizon: no historyEffectQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "horizon: unable to delete all from history_effects")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "horizon: failed to get rows affected by deleteall for history_effects")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o HistoryEffectSlice) DeleteAllG() (int64, error) {
	return o.DeleteAll(boil.GetDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o HistoryEffectSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("horizon: no HistoryEffect slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(historyEffectBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), historyEffectPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"history_effects\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, historyEffectPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "horizon: unable to delete all from historyEffect slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "horizon: failed to get rows affected by deleteall for history_effects")
	}

	if len(historyEffectAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *HistoryEffect) ReloadG() error {
	if o == nil {
		return errors.New("horizon: no HistoryEffect provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *HistoryEffect) Reload(exec boil.Executor) error {
	ret, err := FindHistoryEffect(exec, o.HistoryAccountID, o.HistoryOperationID, o.Order)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *HistoryEffectSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("horizon: empty HistoryEffectSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *HistoryEffectSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := HistoryEffectSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), historyEffectPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"history_effects\".* FROM \"history_effects\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, historyEffectPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "horizon: unable to reload all in HistoryEffectSlice")
	}

	*o = slice

	return nil
}

// HistoryEffectExistsG checks if the HistoryEffect row exists.
func HistoryEffectExistsG(historyAccountID int64, historyOperationID int64, order int) (bool, error) {
	return HistoryEffectExists(boil.GetDB(), historyAccountID, historyOperationID, order)
}

// HistoryEffectExists checks if the HistoryEffect row exists.
func HistoryEffectExists(exec boil.Executor, historyAccountID int64, historyOperationID int64, order int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"history_effects\" where \"history_account_id\"=$1 AND \"history_operation_id\"=$2 AND \"order\"=$3 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, historyAccountID, historyOperationID, order)
	}

	row := exec.QueryRow(sql, historyAccountID, historyOperationID, order)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "horizon: unable to check if history_effects exists")
	}

	return exists, nil
}
